# Pendientes tras la feature 010

La revisión adversarial de cierre de la feature 010 dejó anotadas **doce** cosas no bloqueantes en
`specs/010-tax-output/questions.md`, apartado «5 bis». Después de esa anotación entraron **sesenta
commits** más antes de fusionar, así que varias podían estar ya hechas.

Esto es el resultado de comprobarlas **una por una contra el código fusionado en `develop`**
(2026-09-23, `d4e41e5`). **Es un inventario, no trabajo hecho**: nada de código se ha tocado aquí.
La dirección decide qué entra en la próxima ronda.

**Tres están hechas** (1, 2 y 11) y **nueve siguen vivas**.

| # | Qué era | Estado | Qué cuesta | Qué se pierde si no se hace |
|---|---|---|---|---|
| 1 | El fichero dorado nunca ejercitaba la ventana de dos meses en un fondo: `synthetic-v1.jsonl` está congelado con `fund: "1y"` y `money_market: "1y"` | **Hecha** (`90545c9`), por la vía (b) recomendada: `packages/domain/test/tax/two-month-fund.test.ts` fija a mano las dos lecturas —a dos meses y a un año— con sus trece cifras, y escribe las ventanas explícitas en vez de heredar el valor por defecto. El dorado sigue intacto y no existe ningún `fund-2m-v1.jsonl`. Dos desviaciones de la letra, sin consecuencia: el test vive en fichero propio y no en `wash-sale.test.ts`, y no se añadió la línea a `hand-checked.test.ts` —no cabía, porque ese fichero está atado al `tax-hand-v1.jsonl` congelado, cuya configuración fija el fondo a un año— | — | — |
| 2 | El año mínimo de presentación era 2018 para los tres modelos, con una justificación (el régimen de compensación de la Renta) que no alcanza a un modelo informativo | **Hecha** (`764dd3c`): `FIRST_FILING_YEAR` en `packages/domain/src/schema/events.ts` es ahora un mapa por modelo —`renta` 2018, `720` **2012**, `721` 2023—, con el motivo de cada uno escrito al lado, consumido por `validate.ts` y por `informative/m720.ts`, y con un test que fija los tres valores | — | — |
| 3 | `compact` queda bloqueado para siempre si una huella de presentación no se puede verificar, y compactar es la única vía de migrar el libro a una versión de esquema nueva | **Pendiente.** `packages/domain/src/usecases/compact.ts` rechaza con `CompactRejectedError` ante **cualquier** motivo (`lines`, `digest` y también `unreadable`); no hay salida ni en el dominio (`CompactPlan` no tiene campo) ni en la CLI (`atlas compact` solo acepta los flags globales), ni procedimiento de recuperación escrito en `docs/` | El dominio es poco: un campo en `CompactPlan` y una confirmación en la CLI. Lo caro es que la anotación pide una salida **registrada**, y registrarla es trabajo de esquema —marcar la presentación como no verificable—, más los mensajes de las dos interfaces. La propia anotación lo deja como decisión de la dirección, no como elección del implementador | Un libro con una huella rota, o simplemente ilegible en la versión que declara, se queda congelado en su versión de esquema **para siempre**. El único rodeo es editar el `.jsonl` a mano, que es exactamente lo que la huella existe para detectar |
| 4 | «Se han editado a mano» se dice también cuando el caso es «no verificable»: el dominio distingue `digest` de `unreadable`, pero los dos viajan bajo el mismo código | **Pendiente.** `packages/domain/src/projections/deep-check.ts` emite un solo `filing_fingerprint_mismatch` para los dos, y `IntegrityFinding` no tiene campo de detalles por el que pudiera viajar el motivo. La CLI dice «(editados a mano)» y la web «**Se han editado a mano.** Recupera la copia anterior a la edición» en los dos casos. El mensaje inglés del dominio sí los distingue, y se ve en el detalle técnico | Bajo y acotado: un código propio (`filing_fingerprint_unreadable`) en `deep-check.ts` y su entrada en los mensajes de las dos interfaces, que `tests/messages.test.ts` exigirá. Se arregla junto con el 6, que está en las mismas tres líneas | Se acusa de manipular el libro a quien solo tiene líneas que no se pueden releer en la versión de la huella, y se le manda restaurar una copia de seguridad que no arregla nada |
| 5 | `computed.as_of` solo se valida como fecha: puede ser anterior a `filed_at`, anterior al ejercicio declarado o futura | **Pendiente.** `packages/domain/src/schema/validate.ts` sí comprueba `filed_at` contra el ejercicio y contra `recorded_at`, pero de `computed.as_of` solo exige que sea una fecha. La muestra `SAMPLES.tax_return_filed` sigue con `as_of` (2026-06-20) **posterior** a su `filed_at` (2026-06-18) | Pequeño: tres comparaciones en el bloque de consistencia que ya existe para el evento, con sus códigos y sus dos traducciones. Ningún *fixture* dorado contiene presentaciones, así que no hay nada que regenerar | `as_of` es la fecha con la que la comparación relee el prefijo del libro. Una presentación escrita fuera de `filingProposal` con una fecha absurda se acepta, y el reparto de la diferencia en sus cuatro causas sale falso sin que nada avise |
| 6 | Un mensaje de la comprobación profunda imprime el recuento de líneas donde dice «versión de esquema» | **Pendiente.** `deep-check.ts` escribe `cannot be read at schema version ${check.declared_lines}`, y `declared_lines` es el número de líneas | Tres ficheros, porque `FingerprintCheck` no lleva la versión: añadirle el campo, rellenarlo donde se recalcula la huella y usarlo en el mensaje | El único mensaje que debería decir **desde qué versión** hay que migrar da un número que no es una versión, justo en el camino en que el usuario más lo necesita |
| 7 | La lectura alternativa de un ejercicio no soportado lanza en vez de informar | **Pendiente, y es el que más preocupa.** `taxChain` lanza `tax_year_unsupported`, y la única guardia del repositorio es el `try/catch` de `movedTaxYears`. Quedan sin guardia tres lecturas alternativas, las tres alcanzadas **después** de que la lectura principal haya salido bien: `criterionStakes` y `settingsDiff` (`packages/domain/src/tax/year.ts`) y `figuresOf` (`packages/domain/src/filings/closed-years.ts`). Las tres tratan con elegancia el caso «libro inválido» y ninguna el caso «lanza» | La guardia de `movedTaxYears` extraída a un ayudante y aplicada en tres sitios. Lo que hay que decidir es qué se emite en su lugar: `criterionStakes` ya tiene el vocabulario (`not_quantifiable` con su motivo) y `settingsDiff` necesitaría el equivalente de su `invalid_before` | El informe de un ejercicio **soportado y calculable** se vuelve inobtenible —no degradado: inobtenible— en cuanto una lectura alternativa alcanza por debajo de 2018, y el mensaje culpa al ejercicio que el usuario pidió, que es una afirmación falsa |
| 8 | El aviso de ejercicio cerrado puede callarse: si la lectura anterior es inválida y además el cambio no cae por fecha en el ejercicio declarado, no sale nada | **Pendiente.** `fb0ca0c` partió la condición en dos sentencias por atribución de cobertura y dice él mismo que la conducta es idéntica. Con lectura inválida y cambio fuera de fecha, `closedYearImpact` no empuja nada, y `ClosedYearImpact` no tiene ningún campo con el que decir «no he podido comparar». El test que existe fija solo el caso en que la fecha salva el aviso | En el dominio, un tercer desenlace y un campo en el tipo. El coste está en las interfaces, que consumen el tipo en ocho sitios (cinco en la web, tres en la CLI) | La garantía que la feature vende —«nunca en silencio», escrito en la cabecera de `closed-years.ts`— es falsa en una combinación alcanzable: libro con eventos inválidos, cambio fechado fuera del ejercicio presentado que aun así mueve una cifra declarada. Es el ejemplo que usa la propia cabecera: la recompra de enero que difiere la pérdida de diciembre |
| 9 | La huella de duplicados de una presentación incluye `filed_at`, así que el mismo justificante registrado dos veces con fechas distintas no se detecta | **Pendiente.** La tupla de `packages/domain/src/schema/fingerprint.ts` es `type + filed_at + model + tax_year + receipt_reference`, y contradice al comentario que tiene tres líneas más arriba, que dice que una presentación se identifica por modelo, ejercicio y justificante | El cambio es borrar una línea. Lo caro es que `fingerprint` es un **campo persistido** de un libro append-only: cambiar la tupla cambia la huella de toda presentación ya escrita, así que exige `schema_version = 2` y su migración | El mismo justificante registrado dos veces con fechas distintas no dispara la confirmación de duplicado, y quedan dos presentaciones para una, alimentando las dos la cadena de complementarias y el ancla del ejercicio |
| 10 | La nota N17 omite una fase al resumir la compensación y deja ver una cadena que no cierra | **Pendiente.** `specs/010-tax-output/questions.md` sigue escribiendo «ganancias 462,50; rendimientos −26,00; 26,00 compensados en la fase 1 (límite 115,63); base 175,70», y 462,50 − 26,00 no es 175,70: falta la fase 2, los −260,80 arrastrados de 2027. El motor sí hace las dos, y la cadena está bien escrita en `income-category-default.test.ts` y en `specs/009-tax-engine/questions.md` | Una frase | Quien lea N17 ve una aritmética que no cierra y puede concluir que el motor se salta la fase 2, o que 175,70 está mal. No toca código |
| 11 | Los 200,00 € del criterio #19 dejaban de verse, y al #18 le pasaba lo mismo desde que subió a certeza alta | **Hecha** (`c34222c`): la partición sustituyó al filtro. `TaxYearReport` lleva ahora `settled` además de `doubtful`; la CLI imprime siempre el apartado 10, «Criterios firmes: lo que moverían leídos al revés», y la web pinta una tarjeta propia junto a la de los dudosos. Un criterio firme cuya lectura contraria no mueve nada aparece igual, con su cero y su motivo | — | — |
| 12 | El test antideriva de criterios no ata cada variante a su lectura en las filas que nombran varias | **Pendiente.** `tests/fiscal-criteria.test.ts` comprueba pertenencia al conjunto de la fila, que la certeza más dudosa la lleve alguien y que cada riesgo lo lleve alguien; nunca empareja variante con lectura. Solo afecta a las filas **#2** (siete variantes en el catálogo) y **#24** (cuatro). La cabecera del test ya dice exactamente esto, y lo decía desde antes de la anotación | Medio, y toca un documento de la dirección: la vía viable es llevar la correspondencia a `docs/fiscal-questions.md` en un formato legible por máquina —una fila por variante— y reescribir el bucle del test. Analizar la frase española no es opción | Una variante de #2 o de #24 puede tomar la certeza o el riesgo de su hermana sin que el test lo vea. Desde `c34222c` eso tiene consecuencia visible: la certeza decide si un criterio sale en «en duda» o en «firmes», es decir si al usuario se le dice «nadie sabe cómo se lee esto» o «esto está resuelto, y esto es lo que hay detrás». Hoy lo sostienen los comentarios por variante de `criteria.ts` y la lectura humana |

## Cómo se agrupan

- **3, 4 y 6** son el mismo sitio: el caso `unreadable` de la huella. El 4 y el 6 están en las mismas
  tres líneas de `deep-check.ts` y son baratos; el 3 es el que pide decisión.
- **5, 7, 8 y 9** son huecos del fallo seguro: en los cuatro el motor sabe algo y no lo dice, o lo
  dice mal. El **7** y el **8** son los que cambian lo que el usuario ve.
- **10 y 12** son documentación y test, sin riesgo de cambiar una cifra.

## De paso

Al comprobar el 11 apareció una asimetría entre las dos interfaces que no está en la lista: la CLI
imprime **siempre** el apartado 10 de criterios firmes, y la web envuelve su tarjeta en un `Show`
que la esconde cuando no hay ninguno, sin estado vacío —la tarjeta de los dudosos sí lo tiene—. No
se pierde ninguna cifra, pero es exactamente la clase de divergencia que motiva el
[ADR-0024](adr/0024-fiscal-caveats-are-report-notes.md).
