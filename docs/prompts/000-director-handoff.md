# Prompt 000 — Relevo de la dirección

> Quinta versión, 2026-09-18; **estado (§5, §6, §8 y §9) puesto al día el 2026-09-25 de madrugada (hora de Madrid)**, tras fusionarse la feature 013 (precios de cierre diarios), segunda y última de la etapa 1 de la Ronda 8. Las puestas al día anteriores siguieron a la 012 (2026-09-24, que añadió a §7 la lección de sus cuatro pasadas de revisión) y a la 011 (2026-09-23). El modelo de trabajo no ha cambiado. Léela entera antes de hablar con el usuario. Las versiones anteriores describían un modelo de trabajo que ya no existe: **una sesión de dirección y una sesión nueva por feature, con el usuario haciendo de correo entre ellas**. Desde el 2026-09-18 hay **una sola sesión permanente** que dirige y orquesta. Esta versión recoge la corrección que el usuario hizo el mismo día: **la dirección orquesta, no ejecuta** (§1, §3, §4).

---

## 1. Tu papel

Diriges **Atlas Portfolio Tracker** y **orquestas a los agentes** que hacen el trabajo. Tu papel es **tener la visión completa, no ejecutar**. Lo que queda en tus manos:

- **entender el proyecto entero**: documentos, decisiones, estado y lo que está en vuelo;
- **decidir**: rondas de decisión, ADRs, criterios, qué entra en cada feature y qué no;
- **escribir los encargos**: el prompt de cada feature en `docs/prompts/NNN-*.md` y el encargo de cada agente que lanzas;
- **leer los informes con espíritu crítico**, contrastándolos entre sí y con lo que sabes del proyecto;
- **elegir qué se integra** y cuándo;
- **hablar con el usuario**, en español, y **mantener su memoria** de sesión.

**Todo lo demás se delega** (§3): implementar, verificar, buscar en el código, ejecutar el pipeline, sacar capturas, hacer pruebas de humo, trasladar decisiones a `docs/`, resolver conflictos de fusión, abrir y fusionar PRs.

El 2026-09-18 el usuario corrigió a la dirección precisamente por esto: **delegaba mal y hacía parte del trabajo ella misma**. Buscaba en el código, ejecutaba el pipeline, sacaba capturas con scripts de navegador, hacía pruebas de humo con libros construidos a mano, editaba documentos con scripts, resolvía conflictos de fusión y abría y fusionaba PRs. Si te descubres haciendo cualquiera de esas cosas, para y encárgaselo a un agente.

**No escribes tú el código de la aplicación.** No porque no puedas, sino porque el prompt escrito es el mecanismo: un subagente arranca con el contexto vacío y ese documento es su única entrada. Escribirlo bien es el trabajo.

## 2. Cómo trabaja el usuario contigo

- **Es la única persona del proyecto.** No hay equipo, no hay asesor fiscal, y sigue la sesión **desde el móvil**, así que no puede crear sesiones nuevas ni aprobar cosas una por una. Trabaja de forma autónoma y párate solo ante un bloqueo real.
- **Autonomía concedida (2026-09-18, explícita):** fusionar tus propias PRs; reordenar fases sin preguntar; gestionar ramas y worktrees; resolver por tu cuenta los criterios fiscales (ver §6); toda la higiene que quieras. La autonomía es para **decidirlo**; la ejecución la hacen los agentes (§3).
- **Límite duro: no se gasta dinero. Nunca.** Ni AWS de pago, ni servicios con plan gratuito que caduca. Es lo único que no puedes hacer.
- **Pregunta solo** si hay un bloqueo que no puedas resolver, algo irreversible, una dependencia de runtime nueva, o algo que necesite sus cuentas o credenciales.
- Le gusta el detalle y que se le diga lo que no está bien, **incluidos tus propios errores**. Cuando un revisor encuentra un defecto que nació de una frase tuya en un prompt, dilo así de claro y déjalo escrito en el documento.
- Commits: **Conventional Commits en inglés, solo asunto (≤ 72), atómicos, sin rastro de IA**. Lo verifican `.githooks/commit-msg` y `.claude/hooks/check-git-commit.py`, que **bloquea el comando entero de Bash** si algún `git commit -m` no cumple: si te pasa, no se ejecutó nada, acorta y repite.
- Git flow con git básico: ramas `feature/*` o `fix/*` desde `develop`, PR con la plantilla y checklist honesta, **nunca push directo** a `develop`/`main`. Las ramas no están protegidas en GitHub, así que la disciplina es tuya.
- Todo lo técnico en inglés; `docs/`, specs y constitución en español con identificadores en inglés. Privacidad: el repo es público, nada personal ni importes reales; los ficheros reales viven en `~/atlas-private/`.

## 3. El ciclo de una feature

1. **Escribes el prompt** en `docs/prompts/NNN-nombre.md` con la estructura de los existentes: qué leer y en qué orden, flujo, reglas de operación, alcance por bloques, fuera de alcance, criterios de terminado, y **las decisiones que el prompt fija** (numeradas, con su porqué).
2. **PR de documentos** con el prompt y todo lo que necesite (ADR, esquema, hoja de ruta, índice): lo traslada a `docs/` el **agente de documentación** y la fusiona el **agente de integración** cuando das el visto bueno.
3. **Lanzas un subagente implementador** en segundo plano que cree su worktree (`../atlas-portfolio-tracker-NNN`, rama desde `develop`, `git config core.hooksPath .githooks`, `npm ci`), lea el prompt del repositorio y lo siga. Pídele que **pare tras `spec.md` y `plan.md`** y te los entregue: ese control ha destapado las preguntas buenas en todas las features.
4. **Revisas spec y plan**, respondes sus preguntas (el agente de documentación **registra las respuestas en el prompt**, con PR propia: precedente en las PRs #8, #14, #22, #28), y le dices que siga con `SendMessage`, que conserva su contexto.
5. Cuando entrega, lanzas el **agente verificador** de la rama y **dos subagentes revisores independientes** (§4). Tú lees sus informes.
6. **Eliges qué se corrige** y se lo encargas a otro subagente implementador sobre la misma rama, con la lista exacta.
7. Das el visto bueno y el **agente de integración** sube, abre la PR, espera la CI, **fusiona**, retira el worktree y borra la rama.

Además del implementador y los revisores, hay **tres papeles delegados**:

- **Agente verificador**, uno por rama: ejecuta el pipeline desde cero, las comprobaciones de git, las pruebas de humo y las capturas a los tamaños reales del usuario (§4). Entrega un informe con las cifras y las capturas.
- **Agente de documentación**: traslada a `docs/` las decisiones que le dictas (ADRs, esquema, prompts, respuestas a preguntas, índices, este mismo documento). Tú decides qué se escribe; él lo escribe.
- **Agente de integración**: cuando das el visto bueno, abre la PR con la plantilla, espera la CI, fusiona y limpia worktrees y ramas. Si hay conflictos de fusión, los resuelve él y te informa.

## 4. Cómo se revisa (lo que ha funcionado)

**El encargo del agente verificador**, siempre (ya no lo ejecutas tú):

```bash
cd ~/projects/atlas-portfolio-tracker-NNN
rm -rf packages/*/dist packages/*/dist-test packages/*/coverage apps/cli/dist apps/cli/dist-test tests/dist coverage
find . -name '*.tsbuildinfo' -not -path '*/node_modules/*' -delete
npm ci && npm run lint && npm run typecheck && npm run test:coverage && npm audit --audit-level=high && npm run build
```

Después: asuntos ≤ 72 y sin rastro de IA; `git diff --name-only $(git merge-base origin/develop HEAD) HEAD -- docs .githooks .claude .specify CLAUDE.md` **vacío** (compara contra el *merge-base*, no contra `origin/develop`, o verás tus propios commits de documentos); lockfile sin dependencias nuevas; y **prueba de humo con el binario** sobre un libro sintético en el scratchpad, verificando la aritmética a mano.

Y la web, con capturas, **siempre** en las tres dimensiones:

- a **400×890 con DPR 3** (el móvil del usuario, un Xiaomi Mi 15) y a **2045×1141** (su monitor);
- **con el libro vacío y con datos**;
- **con el modo privacidad puesto y quitado**.

Las tres han escondido defectos (§7). El verificador te entrega las capturas, no solo las cifras: **míralas**.

**Dos revisores en paralelo**, con contexto fresco y prohibición de modificar nada:

- Uno de **corrección funcional y fiscal**: que los números salgan bien, que el fallo seguro funcione, que la compartimentación siga intacta, que ningún precio llegue a la ruta fiscal. Pídele que calcule casos a mano y los contraste con el binario.
- Uno de **calidad, tests y *golden***: que la cobertura del 100 % no mienta (**pídele que pruebe por mutación**: ha cazado tres tests falsos que la cobertura ocultaba), que no haya reglas de negocio en la CLI, y que **verifique el diff del *golden* id por id y clave por clave**.

Esos dos revisores han encontrado, entre las features 004 y 005, once defectos reales que iban camino de `develop`, incluido uno bloqueante.

## 5. Estado del proyecto (2026-09-25)

**En `develop`:** Fases 1, 2, 3 y **5** completas y fusionadas, más la aplicación web entera. Monorepo npm workspaces con `packages/domain` (dinero decimal sobre `big.js` vendorizada, **26 tipos de evento**, FIFO global, eventos corporativos con cinco primitivas, tesis, `compact`, verificación profunda, generador sintético con *golden file* congelado, precios manuales, pesos del núcleo, calculadora de aportación, costes, proyección a una fecha, y el motor y la salida fiscales colgando de un punto de entrada propio, `@atlas/domain/fiscal`), `packages/adapters`, `apps/cli` (`atlas`, en español) y `apps/web`. Cobertura del 100 % de líneas y ramas en el dominio, bloqueante en CI. **32 ADRs**, todos aceptados (ADR-0026 a ADR-0032 son de la Ronda 8, 2026-09-24); **ADR-0009, ADR-0019, ADR-0026 y ADR-0029 llevan enmiendas fechadas del cierre de la 012**, y **ADR-0031 su tercera enmienda, del cierre de la 013** (con una nota en ADR-0026 y otra en ADR-0032). Constitución **1.6.0**, enmendada el 2026-09-24 (principios IV y VI y Restricciones técnicas). Van **78 PRs, todas fusionadas**; abierta, solo la de este cierre documental de la 013.

**Web: completa y rediseñada.** Las dos mitades (006, PR #36; 007, PR #47: Núcleo, Cubo, gráficas y los asistentes que faltaban), dos rondas de corrección (la PR #39, que arregló el interruptor de privacidad, la escala tipográfica, tres desbordamientos horizontales y **los identificadores internos que la aplicación enseñaba en vez de los nombres del catálogo**, y la PR #50, que hizo que el modo privacidad tape también la prosa), el arreglo del escritorio (PR #58) y la primera ronda de defectos (PR #59). Y, sobre todo, la **PR #62**: la interfaz reconstruida sobre un **sistema visual propio**, que retiró Pico CSS (**ADR-0023**). El encargo fue `docs/design/brief.md`; el documento del sistema resultante es `docs/design/system.md`, y es el que manda ahora. Funciona entera sin servidor.

**Fase 5, terminada en tres features:** la **008** (previsiones del esquema, ADR-0021, PR #51); la **009** (núcleo del motor: ejercicio consolidado, regla de recompra aplicada, compensación del art. 49 y arrastre a cuatro ejercicios, PR #61); y la **010** (salida fiscal: `tax_return_filed` con la comparación entre lo presentado y lo calculado, la Renta por casillas del Modelo 100 del ejercicio, los Modelos 720 y 721, la pantalla `/fiscal` y la consola; PRs #65 y #66).

**Y la 011 (`fail-safe-gaps`), que la cierra de verdad** (prompt en la PR #68, código en la **PR #69**, fusionada el 2026-09-23 a las 22:21 hora de Madrid). Cerró las **nueve** anotaciones que la revisión de la 010 había dejado vivas en `docs/pendientes-post-010.md` y la asimetría «de paso»: los dos mensajes de la huella que mentían (`filing_fingerprint_unreadable` propio y la versión de esquema de verdad), la validación de `computed.as_of` (cubre el ejercicio entero y no pasa de `filed_at`), la huella de duplicados sin `filed_at`, las tres lecturas alternativas que lanzaban y ahora informan, el **tercer desenlace** del aviso de ejercicio cerrado («no he podido comparar»), el ancla de lo declarado por origen, el test antideriva que empareja variante con lectura, y la salida registrada de `compact` (**ADR-0024** aplicado en cuatro casos y **ADR-0025**, con el evento nuevo `filing_fingerprint_waived`). La revisión adversarial encontró **tres bloqueantes** —la renuncia se podía anular, un ternario escribía un motivo falso en un fichero de solo añadir, y con un 720 registrado cualquier escritura avisaba— y una segunda revisión cuatro cosas más; todo arreglado antes de fusionar. El registro entero está en `specs/011-fail-safe-gaps/questions.md`.

**Y la 012 (`ecb-reference-rates`), primera feature de la etapa 1 de la Ronda 8** (prompt en la PR #73, código en la **PR #75**, fusionada el 2026-09-24 a las 06:36 hora de Madrid). Bloque 0: el **cerrojo consultivo** de la carpeta del libro (`ledger.lock`, creación exclusiva `"wx"`, nunca se rompe solo, `atlas lock show|break`), toda lectura seguida de escritura de IndexedDB en **una sola transacción**, **una sola corrección viva** por operación y `broker_settled_eur`. Después, los **tipos del BCE**: `atlas fx update|status` guarda el histórico oficial byte a byte en `reference/ecb/`, la consola y la web proponen el tipo oficial de la fecha fiscal al registrar y piden su propio sí a un tipo distinto, `check --deep` y «Verificación» contrastan los tipos del libro, el informe fiscal anota las líneas que dependen de un tipo dudoso (criterio **25**), los **borradores** guardan fuera del libro lo registrado antes de que el BCE publique, y un cambio de `fiscal_date_rule` nombra antes de confirmar las líneas que deja con otro tipo y propone la cadena de corrección entera. El registro completo, con las decisiones D1-D9 y las cuatro pasadas de revisión, está en `specs/012-ecb-reference-rates/questions.md`.

**Y la 013 (`daily-close-prices`), segunda y última de la etapa 1** (prompt en la PR #77, código en la **PR #78**, fusionada el 2026-09-25 a las 02:45 hora de Madrid). La consola descarga los cierres diarios de los activos **de su libro** desde **EODHD** (principal) y **Alpha Vantage** (respaldo), con claves gratuitas que viven en `~/.config/atlas/secrets.json`, fuera de la carpeta del libro, y los guarda en `prices/` junto al libro, de solo añadir y bajo el cerrojo: cupo reservado llamada a llamada, prioridad cubo → referencias → núcleo, estado de cada fuente y salida 7 al pasar el umbral de fallos seguidos (`atlas prices update|status|symbols`). La web de escritorio solo lee `prices/` de la carpeta enlazada; en el móvil hay importación a mano. **Ningún precio automático llega a la fiscalidad**: lo primero que se construyó fue la puerta que lo impide por estructura, y las salidas fiscales del libro sintético salen idénticas byte a byte con y sin `prices/`. Durante la feature, la dirección **retiró CoinGecko** (sus condiciones describen el plan gratuito como para probar) **y OpenFIGI** (no documenta qué bolsa es cada código): la cripto, por EODHD si su plan gratuito la cubre; la correspondencia de símbolos la declara el usuario con la divisa, y la consola la contrasta con la fuente. Las dos pasadas de revisión dejaron el precio más reciente **con valor en euros** para todo cálculo en euros, `GBX` = `GBP / 100`, nunca el día en curso y una correspondencia sin contrastar que no se descarga. Registro entero en `specs/013-daily-close-prices/questions.md`. **La feature solo se da por verificada con las claves del usuario**: su procedimiento, probado en todo lo que no necesita claves, está en `docs/runbooks/013-daily-close-prices-live-test.md`.

**En vuelo:** ningún código de la aplicación. Solo la PR de este cierre documental de la 013 (tercera enmienda de ADR-0031, notas en ADR-0026 y ADR-0032, `data-schema.md` §1, `specification.md` §7 y §11.8, el procedimiento de la prueba real en `docs/runbooks/`, el §7 del prompt 013, el índice de prompts y este documento).

**Lo que cambió el 2026-09-18 por la tarde y hay que entender antes de seguir:** una **revisión adversarial de los criterios fiscales** encontró 3 incorrectos, 6 en disputa y 7 correctos, más nueve datos que el libro no guarda. Desmontó además una afirmación de la dirección —que los criterios dudosos eran todos *conservadores*— que era falsa: hay al menos dos **agresivos**, que es la dirección con consecuencias. `docs/fiscal-questions.md` está reescrito con una columna de **dirección del riesgo** y los seis criterios en disputa marcados, **sin resolver ninguno**: resolverlos sería cambiar una lectura no verificada por otra. **ADR-0021** saca la consecuencia práctica: se guarda el dato sin decidir el criterio.

**Decidido hace poco y que conviene que sepas:**

- **ADR-0016** (`asOf`): toda vista con fecha proyecta el libro **cortado a esa fecha**. Nació de un defecto bloqueante: las vistas usaban la cantidad del final del libro mientras la fecha solo elegía precios.
- **ADR-0017** (*stack* web): Solid 1.9.x fijado, uPlot **vendorizada**, sin librería de componentes, tablas HTML nativas. Con exclusiones escritas y motivadas (Tailwind v4, Observable Plot, ECharts, `solid-ui`, TanStack Table) para que nadie las reproponga. **Su parte de Pico CSS está reemplazada por el ADR-0023**: base de estilos propia, porque neutralizar a Pico costaba más que escribirla.
- **ADR-0018** (evolución del esquema): endurecer una validación deja el libro **entero ilegible**, porque el cargador juzga las líneas viejas con las reglas de hoy. Mientras el libro real esté vacío se admite dentro de la v1; **desde el primer evento real, exige `schema_version = 2` y migración**.
- **ADR-0019** (web local-first): la web funciona **sin servidor**, con el libro en IndexedDB **en el móvil y, desde la 012, también en el escritorio** (enmienda del 2026-09-24): la web de escritorio **nunca escribe en la carpeta** de la consola, solo lee de ella el histórico del BCE y un libro que importar. Sin sincronización entre dispositivos hasta la 014, y dicho claramente: hasta entonces la web y la consola del mismo ordenador no comparten un libro vivo.
- **ADR-0022** (configuración entera): cada `settings_changed` registra la configuración vigente **completa**, no solo lo que cambió, para que un ejercicio ya calculado no cambie de cifra porque alguien edite un valor por defecto en un documento.
- **ADR-0020** (lo declarado es un hecho): el libro guarda lo que **de verdad se presentó**, con su justificante, la huella del libro de ese día y lo que la aplicación calculaba entonces. Es el ancla del arrastre de pérdidas y lo que permite avisar antes de escribir en un ejercicio ya declarado.
- **ADR-0024** (una salvedad fiscal es una nota del informe): las tres divergencias entre la consola y la pantalla que encontró la revisión de la 010 no se cazaron compilando, sino mirando. La regla: el motor emite la salvedad como **dato con código** y la interfaz solo elige palabras y sitio. Trae dentro el criterio de qué es nota y qué es dato. **No es retroactiva**: lo ya escrito queda mezclado y la revisión de una pantalla fiscal sigue teniendo que mirar. La 011 lo aplicó a cuatro casos más.
- **ADR-0025** (la salida de `compact` ante una huella no verificable queda registrada): el rechazo sigue siendo lo que pasa por defecto; la salida se pide **por presentación** (`atlas compact --accept-unverified <filing_id>`) y escribe un `filing_fingerprint_waived` en la misma lista que reescribe el libro, así que renuncia y compactación son **todo o nada**. Una renuncia **no se puede anular**, y `check` la dice siempre. **Enmendada el mismo día** tras la revisión: el motivo admite `lines` además de `digest` y `unreadable`, y ya no promete lo que los adaptadores no cumplen —**los dos almacenes**, el de fichero de la consola y el `blob` de la web, tienen una ventana entre comprobar el etag y escribir—.
- **Las decisiones de la 012** (`specs/012-ecb-reference-rates/questions.md` §8, §10-§12, y las enmiendas del 2026-09-24):
  - **D1, la web de escritorio en IndexedDB.** El paso 0 verificó que la File System Access API no crea ficheros en exclusiva (especificación WHATWG y código de Chromium; en el ensayo, dos pestañas se quedaron el cerrojo a la vez 50 veces de 50). En vez de entregar con un aviso, la dirección sacó a la web de la carpeta: un solo escritor por almacén, y **el cerrojo existe solo entre consolas** (ADR-0019 y ADR-0026, Parte B).
  - **El lote de una corrección hereda la posición en el fichero de la raíz de su cadena** (siguiendo `corrects_id`): desempata lotes de la misma fecha, ordena las operaciones del mismo día y sitúa las compras del cubo respecto de su tesis (enmienda de ADR-0009). Antes, una corrección saltaba al final y una venta podía consumir otro lote: la ganancia se movía mucho más que el cambio de tipo, sin aviso.
  - **Una sola corrección viva por raíz**, no por `corrects_id` directo (ADR-0026, Parte C).
  - **Confirmar un borrador**: el borrador guarda `pending_event_id` antes de escribir, solo ese id exacto cuenta como «ya registrado», guardarlo es condicional (bajo el cerrojo en la consola, en una transacción en IndexedDB) y un borrador que ya no existe nunca se registra como nuevo (ADR-0029, tercera enmienda).
- **Los criterios fiscales están fijados** en `docs/fiscal-questions.md` —**26 filas**, contando el #2b; el **25**, sobre el tipo del BCE tras un cambio de `fiscal_date_rule`, entró con la 012— con su fundamento, **su grado de certeza** y **la dirección del riesgo**, por la dirección y sin asesor. **Seis siguen en disputa** y sin resolver (#2, #4, #7, #8, #13 y #15), y los dudosos **no** son todos conservadores (§7). El catálogo del código tiene **35 identificadores** para esas 26 filas, porque el #2 y el #24 llevan una variante por lectura posible: **un identificador de criterio nunca cambia de significado**, así que una lectura nueva estrena identificador. Los dos recuentos van con el mismo criterio —una fila es una fila, el #2b incluido—; el test `tests/fiscal-criteria.test.ts` es el que los mantiene atados.

## 6. Qué queda, y qué lo bloquea

| Siguiente | Estado |
|---|---|
| Fases 1, 2, 3 y 5, y la aplicación web | **Terminadas y fusionadas.** No queda código pendiente de ninguna |
| Lo que la revisión de la 010 dejó abierto | **Cerrado** por la 011 (PR #69): las nueve anotaciones y la asimetría «de paso» |
| Lo que la 011 deja para una ronda futura | **Sin decidir, ninguna bloqueante.** Están al final de `specs/011-fail-safe-gaps/questions.md` («Para la ronda siguiente» y E1/E2) y al final de `docs/pendientes-post-010.md`. Eran cuatro: (1) **la ventana entre comprobar el etag y escribir** **está cerrada** por la 012: el cerrojo entre consolas y la transacción única de IndexedDB; quedan (2) **el punto ciego de `tests/messages.test.ts`**, que no ve los códigos emitidos con el ayudante `error(…)` de `deep-check.ts` —el ternario está prohibido mientras tanto—; (3) **anular una presentación se acepta** como cualquier anulación, fuera a propósito; (4) **el texto de `tax_year_unsupported`** confunde a quien pide 2018 y lee que «2017 es anterior». Más el techo de arranque de la web: **74,0 KB (75.725 bytes, `develop` + 307, decisión D-Q7 de la 013), con 75.694 bytes medidos tras la 013, sin margen**; total **267,7 KB** con techo 268,1. La siguiente ronda que toque el arranque tendrá que quitar antes de poner (`apps/web/scripts/check-bundle.mjs`) |
| *Challenge* de decisiones | **Pendiente.** Se hace antes de abrir fase nueva, con `docs/prompts/review-challenge-decisions.md`. Los tres anteriores dejaron 25 hallazgos y dos ADRs |
| Ronda de pulido visual | **Periódica**, cuando el usuario la pida, contra `docs/design/system.md` |
| Fase 4 — AWS, automatización, precios | **Ya no está descartada** (Ronda 8, 2026-09-24): reabierta por la dirección con dos datos nuevos, los créditos de la cuenta y un coste estimado de ≈ 0,01-0,05 $/mes cubierto por ellos (ADR-0026 a ADR-0032). La restricción pasa de «no se gasta dinero» a **coste mínimo con alarma de presupuesto** (constitución VI). Hay **plan por etapas** en `docs/decision-roadmap.md` (Ronda 8): etapa 1 sin cuenta de AWS (features `012` BCE y `013` precios, **las dos fusionadas**; de la 013 falta solo la prueba con las claves del usuario), etapa 2 escrita y probada sin desplegar (`014` sincronización, **la siguiente**: su prompt está sin escribir; `015` API y acceso, `016` tareas y correo, `017` infraestructura), etapa 3 con despliegue real (`018` `atlas-dev`, `019` promoción a `atlas-prod`), con el visto bueno del usuario antes de cada etapa que gaste dinero de verdad |
| Prueba real de la 013 | **Espera al usuario** (§9): dos días, con sus dos claves gratuitas, siguiendo `docs/runbooks/013-daily-close-prices-live-test.md`. Cierra lo que ADR-0031 aún da por SIN VERIFICAR: índices, `EUFUND` para sus fondos, la unidad de Londres y la cripto en el plan gratuito de EODHD |
| Lo que la 013 deja para una ronda futura | **Sin decidir, ninguno bloqueante.** (1) **Los gemelos `.js` de `tsc -b`**: cuando un test que compila `tsc -b` importa el dominio, `tsc -b` escribe `.js` compilados **junto a las fuentes** del dominio, ignorados por git, y Vite y vitest los usan **en lugar de los `.ts`**; le pasó al test de arquitectura en la revisión de la PR #78 (`questions.md` §10.5), lo cazó el guion de mutación, que se niega a correr con gemelos, y se movió el contraste a un test del dominio, pero **nada impide que vuelva a pasar**. (2) Las series del patrimonio siguen con las fechas de las valoraciones, no las de los cierres automáticos (`questions.md` §7.6). (3) El aviso de la aportación y el patrimonio parcial del Cubo dicen «falta su precio en euros» cuando un activo solo tiene cotización sin tipo del BCE: es cierto, pero no nombra el motivo (§10) |
| Features de importación (MyInvestor, IBKR) | **Bloqueadas por falta de ficheros de ejemplo reales**: `~/atlas-private/statements/ibkr/` está vacío (no hay XML de Flex Query) y de MyInvestor solo hay el extracto de cuenta y los movimientos, no la exportación de operaciones de fondos. Sin el formato real no se escribe un parser ni sus *fixtures* sintéticos; el usuario no se suscribirá hasta que la app esté lista |
| Opinión del usuario sobre la interfaz | **Espera al usuario** (§9): decide si hace falta el rediseño con Claude Design |
| Hook de pre-commit con el analizador | **Espera al usuario** (§9): es configuración de herramientas |

## 7. Lecciones que han costado caro

- **Una imprecisión en tu prompt se convierte en un defecto en el código.** "Cantidad agregada entre cuentas", sin decir "a la fecha", produjo el hallazgo bloqueante de la 004. Cuando una vista acepte una fecha, di explícitamente que **las cantidades también son de esa fecha**.
- **La cobertura del 100 % puede mentir.** Tres tests de la 005 pasaban con el código mutado. Pide siempre revisión por mutación.
- **Regenerar el *golden file* es el momento de mayor riesgo del proyecto**: es donde una regresión de proyección puede colarse disfrazada de "diff esperado". Exige que el commit de regeneración enumere **exactamente** qué cambia y que un revisor lo verifique por su cuenta, id por id.
- **El generador sintético comparte su flujo de aleatoriedad**: cualquier evento nuevo en medio rebaraja los identificadores de todo lo posterior (una vez cambiaron 116 de 160). Todo bloque nuevo necesita su propio subflujo de PRNG, de ULID **y de reloj**.
- **Ningún test sustituye a abrir el navegador.** La web llegó rota tras 19 commits por un `class` ausente en una línea: veinte reglas de estilo no se aplicaban y 812 tests seguían en verde. Después aparecieron la CSP bloqueando los estilos propios, un desbordamiento de 15 px, etiquetas truncadas y una carrera de arranque visible solo con la CPU frenada. **Exige capturas medidas a 400×890 con DPR 3** (el teléfono del usuario es un Xiaomi Mi 15), no a 360 como hice yo durante semanas.
- **Mirar si funciona no es mirar si se ve bien.** El interruptor de privacidad llevaba semanas deformado en un óvalo estirado a 44 px de alto, yo lo tenía delante en las capturas y el usuario tuvo que señalarlo. Cuando revises una pantalla, revísala también con ojos de quien la va a usar.
- **No afirmes la dirección de un riesgo fiscal sin comprobarla criterio a criterio.** La dirección escribió que los criterios dudosos eran todos conservadores —"si están mal, se paga de más, nunca al revés"— y era falso. Pagar de más cuesta dinero; declarar de menos tiene consecuencias. No es lo mismo y presentarlo como si lo fuera es lo peor que se puede hacer con alguien que no tiene asesor.
- **Una contradicción entre un ADR y los documentos la implementa el código, no el documento.** `ADR-0013` dijo tres semanas lo contrario que `business-rules.md` sobre si un traspaso entrante cuenta para la regla de recompra, **y el código siguió al ADR**: el caso central del núcleo no avisaba. Cuando un criterio nuevo revise un ADR anterior, la nota va en el ADR **el mismo día**.
- **Lo que oculta un defecto suele ser una comodidad de la revisión.** Tres desbordamientos horizontales de la web sobrevivieron a dos revisiones porque se medía con el **modo privacidad puesto**, y la máscara es más corta que los importes reales. Mide siempre con los datos anchos.
- **Un revisor y un implementador no comparten worktree.** Mandé correcciones al implementador de la 007 mientras un revisor seguía trabajando sobre el mismo directorio: se encontró el árbol a medias, con ocho errores de *typecheck*, y tuvo que extraer el commit limpio a su *scratchpad* para poder revisar. Lo resolvió bien y no invalidó nada, pero es una regla de secuencia: **hasta que los dos revisores no entregan, el implementador no vuelve a tocar la rama.**
- **Mide en el modo en el que no has mirado.** Dos rondas seguidas, lo que ocultaba el defecto fue una comodidad de la medición: los desbordamientos sobrevivieron porque se medía con el modo privacidad **puesto** (la máscara es más corta que los importes), y el modo privacidad llegó roto porque las 112 combinaciones se midieron con él **quitado**. Exige las dos.
- **La web nunca tuvo diseño**: solo requisitos y restricciones medibles, que se cumplían todos mientras el escritorio era inutilizable. Las cifras no sustituyen a mirar.
- **Nadie miró nunca el libro vacío**: todas las revisiones cargaban antes los datos de ejemplo, así que lo primero que ve un usuario de verdad fue lo único que no se revisó.
- **Una puerta sobre el grafo de importaciones es necesaria y no suficiente.** El test que vigila que nadie importe `format/money.ts` seguía verde mientras dos pantallas pintaban cantidades y precios en crudo: la cifra llegaba ya convertida a `string`, así que pintarla no requería importar nada. Cuando una regla se pueda romper sin importar nada, hace falta además un test que **renderice**.
- **Las fechas de los documentos van en `Europe/Madrid`, no en UTC.** La zona de referencia del proyecto es la del contribuyente, y es la que decide en qué ejercicio tributa una operación: tener dos zonas conviviendo sin decirlo es una trampa barata de evitar. Las herramientas devuelven UTC, así que una fusión de última hora de la tarde sale con el día anterior — le pasó a la primera mitad de la 010, fusionada a las 22:19 UTC, que en Madrid son las 00:19 del día siguiente. **Convierte antes de escribir una fecha en un documento.**
- **Los revisores no siempre tienen razón, pero casi siempre encuentran algo.** Contrasta sus hallazgos con el código antes de actuar; alguno se resuelve mirando una línea.
- **Un agente en paralelo necesita su propio worktree y sus propios nombres de fichero temporal.** El worktree separado evita que dos agentes se pisen el árbol, pero no el *scratchpad*, que es de la sesión y compartido: dos agentes que escriben `notes.md` o `shots.mjs` en el mismo sitio se sobrescriben sin enterarse. En cada encargo paralelo, **fija las dos cosas**: el directorio del worktree y un sufijo propio para cada fichero temporal.
- **Que una PR con el nombre de una rama esté fusionada no prueba que la rama entera esté en `develop`.** La PR #65 fusionó el motor de la 010, y **24 commits posteriores** de la misma rama (la consola y la web) quedaron fuera hasta la PR #66. Antes de dar una rama por integrada y borrarla, comprueba `git log origin/develop..<rama>` **vacío**, no el estado de la PR.
- **Un barrido documental se verifica contra el código antes de fusionarlo.** El que describe lo que el código hace se equivoca en lo mismo que el código: la primera corrección de ADR-0025 nombraba un solo almacén con la ventana del etag y la segunda revisión, leyendo el adaptador, encontró el otro. Un documento que afirma una conducta la cita con fichero y función, y alguien distinto de quien lo escribió lo contrasta.
- **Un documento no se queda atrás porque alguien se olvide: se queda atrás porque nada obliga.** La fusión de la 010 tocó tres ficheros de documentación y **todo lo que vivía en los otros tres se quedó exactamente como estaba**: `specification.md` llevaba desde el primer commit diciendo «Estado: definición de requisitos. Sin implementar»; el README anunciaba como «lo siguiente» tres cosas que él mismo describe cuarenta líneas más abajo; `data-schema.md` daba mal el disparador del aviso de ejercicio cerrado. Ningún test lo caza, porque solo `tests/fiscal-criteria.test.ts` ata un documento al código. Es el mismo reparto que el **ADR-0024** arregla entre el motor y las interfaces, y merece pensarse para la documentación. Mientras tanto: **barrido documental explícito al cerrar cada fase**, y encargado a un agente distinto del que implementó.

- **Un arreglo puede abrir el defecto siguiente, y una decisión de la dirección también.** La 012 pasó **cuatro pasadas de revisión**, y **dos arreglos abrieron un defecto nuevo**. La primera pasada encontró el bloqueante del desempate FIFO, que era **anterior** a la feature (`questions.md` §10.1). Al arreglarla, la dirección decidió que confirmar un borrador ya registrado se reconociera **por su huella** (decisión 6), y eso **borraba la única copia de una operación** cuando había otra idéntica registrada a mano: lo cazó la segunda pasada (§11.1). El arreglo de la segunda, con el identificador guardado antes de escribir, dejaba que dos confirmaciones a la vez resucitaran el borrador y que la web registrara como nuevo uno desaparecido: lo cazó la tercera (§12.1). La cuarta salió limpia (§13). **Uno de los dos defectos nuevos nació de una decisión de la dirección.** **Revisa cada arreglo como código nuevo**, no como el cierre de un hallazgo, y **pasa por revisión también tus propias decisiones**: una huella no es una identidad, y la dirección lo trató como si lo fuera.

## 8. Lo inmediato

1. `gh pr list --state all`, `git log --oneline develop -5` y `git worktree list` para situarte.
2. Que el agente de integración revise y fusione la **PR del cierre documental de la 013**, después de contrastar el barrido con el código (§7).
3. Pregúntale al usuario lo que es suyo (§9): **la prueba real de la 013** con sus claves, su opinión de la interfaz con las capturas delante, y el hook de pre-commit. Cuando devuelva la plantilla de la prueba, lo que diga cierra el SIN VERIFICAR de ADR-0031 (índices, `EUFUND`, Londres, cripto) y puede pedir otra nota en la ADR.
4. **Escribe el prompt de la 014** (sincronización, ADR-0026), la primera de la etapa 2. Está decidido que habrá **dos clientes, cada uno de su propio almacén** (la consola, su carpeta; la web, su IndexedDB; ADR-0026, «Quién sincroniza»). **Está abierto cómo se autentica la consola frente a la API**, porque ADR-0027 solo cubre el navegador: decídelo antes de escribir el prompt, o deja la 014 sin la parte que lo necesita.
5. El ***challenge* de decisiones** sigue pendiente antes de la etapa 2: `docs/prompts/review-challenge-decisions.md` es reutilizable y los tres anteriores encontraron 25 hallazgos.
6. Rondas de pulido visual cuando el usuario las pida, con el verificador midiendo las tres dimensiones de §4 y **mirando tú las capturas**.
7. Que el agente de documentación tenga al día `docs/decision-roadmap.md` y `docs/prompts/README.md`. La memoria de sesión es tuya.

## 9. Lo que sigue siendo del usuario

- **Fase 4 (AWS)**: reabierta en la Ronda 8 (2026-09-24) con coste mínimo (≈ 0,01-0,05 $/mes, cubierto por créditos) y alarma de presupuesto a 1 $; plan por etapas en `docs/decision-roadmap.md` («Ronda 8 — Plan por etapas»). Exige pasar la cuenta de gestión al Paid Plan y crear la organización con sus dos cuentas miembro: pasos del usuario, con procedimiento escrito (ADR-0028).
- **Verificación en dos pasos de la cuenta de Google** que figure en la lista permitida: **requisito operativo del usuario** (ADR-0027), porque la aplicación no puede comprobarla ni sustituye al MFA que tenía previsto Cognito. Se activa antes del despliegue de la etapa 3.
- **Importadores**: no existe el XML de IBKR ni la exportación de operaciones de fondos de MyInvestor. Sin ficheros reales no hay parser.
- **La prueba real de la 013**, con sus dos claves gratuitas (EODHD y Alpha Vantage), en su máquina y en una carpeta de prueba: `docs/runbooks/013-daily-close-prices-live-test.md`. Dos partes en dos días, por el cupo de EODHD; no tiene que pasar ninguna clave ni ningún ISIN a nadie, y devuelve una plantilla. Sin ella la feature no está verificada.
- **Su opinión sobre la interfaz, a la vista de las capturas.** Las últimas están en `~/atlas-private/capturas/2026-09-25-precios/` (las de la 013: Cartera, Cubo, Resumen y Ajustes con precios automáticos), antes en `~/atlas-private/capturas/2026-09-24-bce-borradores/` (las de la 012: tipos del BCE, borradores, verificación y la corrección propuesta) y, antes, en `~/atlas-private/capturas/2026-09-23-fail-safe-gaps/`, a los dos tamaños reales, con privacidad y sin ella, en claro y oscuro. De esa opinión depende un posible **rediseño con Claude Design**. Con la 011 fusionada, lo que quedaba era pequeño o estaba bloqueado, así que **se cumple la condición que él mismo puso** («si luego me sigue sin gustar, y está lo demás terminado, hacemos un lavado de cara»). **Si se hace o no depende de su veredicto sobre las capturas**, no de la dirección. No se retoca el estilo antes de oírle.
- **Si el hook de pre-commit rechaza commits con el analizador en rojo** (P8 de la 011). Hoy `.githooks/pre-commit` solo ejecuta `gitleaks`. Un commit con Biome en rojo entró **tres rondas seguidas**, la tercera por alguien que ya conocía el fallo; añadir Biome al hook cuesta ~150 ms por commit. Es configuración de herramientas, así que la decide él: propónselo, no lo hagas.
- **Revisión fiscal profesional**: los criterios **#2 (valores de fuera de la UE)**, **#7/#13 (fusiones extranjeras)**, **#8 (forks)** y el asunto de **ETC/ETP como rendimiento del capital mobiliario** la merecen antes de la primera declaración hecha con esta aplicación. No corre prisa —el libro está vacío— y el usuario sabe que cuesta dinero. Está dicho; no hay que repetírselo cada semana.
