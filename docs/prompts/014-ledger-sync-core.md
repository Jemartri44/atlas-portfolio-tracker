# Prompt 014 — Feature `014-ledger-sync-core`

> Copia este texto íntegro al asistente implementador, o indícale que lea `docs/prompts/014-ledger-sync-core.md`.
>
> **Requisito previo, ya cumplido.** Las features 012 y 013 están fusionadas en `develop` (PR #75, #78 y #80), con sus cierres documentales. **ADR-0033** (el token de dispositivo de la consola) está **aceptada** desde el 2026-09-25 (PR #81), y los documentos que cambiaban al aceptarla se actualizaron con este prompt, que además **crea `docs/api.md`**, el contrato HTTP que ADR-0026 y ADR-0033 encargan a la 014.
>
> **Esta feature es la primera de la etapa 2 de la Ronda 8: la nube escrita y probada, sin desplegar nada.** Ni cuenta de AWS, ni una petición HTTP, ni un céntimo. Construyes **el motor de la sincronización** —en el dominio y en los dos clientes— y lo pruebas contra un **remoto simulado** que imita la escritura condicional de S3. La API de verdad, el inicio de sesión, los botones y las órdenes de sincronizar son de la **015**: así lo reparte `docs/decision-roadmap.md` (Ronda 8, «Plan por etapas»), y **si este prompt y la hoja de ruta discrepan, manda la hoja de ruta** (§6.1 (a) dice dónde se ha ajustado).
>
> **Hay preguntas abiertas**, marcadas **[PENDIENTE]** en §6.2. Ninguna bloquea el bloque 0 ni el plan; **las que bloquean un bloque lo dicen**, y ese bloque no se implementa hasta que la dirección conteste en §7.
>
> **§7 está vacío a propósito**: es donde la dirección responderá a las preguntas de §6.2 y a las tuyas, y donde se anotarán los errores de este prompt, como en los trece anteriores.

---

Eres el asistente implementador del proyecto **Atlas Portfolio Tracker** (`~/projects/atlas-portfolio-tracker`). Vas a construir **el núcleo de la sincronización del libro entre dispositivos** (ADR-0026, opción 2): las operaciones del puerto `LedgerStore` que escriben líneas crudas, el caso de uso puro que reaplica una cola sobre el remoto y acepta o retiene línea a línea, el marcador, lo retenido y lo descartado, los dos clientes —la consola sobre su carpeta y la web sobre su IndexedDB—, la negativa de `compact` en una carpeta sincronizada, y un remoto simulado que cumple `docs/api.md` §5 sin HTTP. **Nada de esto puede perder una línea del libro ni mover una cifra fiscal, y lo primero que construyes son los tests que lo demuestran.**

## 0. Cinco cosas que tienes que entender antes de leer nada más

**Perder una línea del libro es lo peor que puede pasar en esta aplicación, y la sincronización es la pieza que más líneas mueve.** Hasta hoy, una línea escrita solo se tocaba por `compact`. Desde esta feature, las **pendientes** de un dispositivo tienen **posición provisional**: al sincronizar pueden quedar detrás de lo que llegó del remoto, o salir del libro local hacia `sync/held.jsonl`. ADR-0026 lo permite con tres garantías que tienes que convertir en tests antes que en código: **una línea sale de la cola solo después de comprobar que está en el remoto, byte a byte** (paso 5); **`held.jsonl` se escribe y se asegura en disco antes de quitar la línea del libro local**, de modo que un corte la deja en los dos sitios y nunca en ninguno (Parte B, segunda enmienda); y **lo retenido nunca se sube solo ni se pierde**.

**Ningún número fiscal cambia por sincronizar.** La sincronización mueve bytes; no interpreta nada. Un libro sincronizado y el mismo libro sin sincronizar —los mismos bytes, uno con `sync/` al lado y otro sin él— dan **la misma salida fiscal, byte a byte**, y nada de `tax/` ni de `informative/` puede alcanzar un módulo de sincronización. Lo que **sí** puede cambiar, y ADR-0026 lo acepta (Consecuencias), es el **orden** de dos operaciones del mismo día registradas en dos dispositivos: la salida fiscal después de sincronizar es la del fichero **reordenado**, no la de antes. El test lo dice así, sin prometer más.

**Dos clientes, uno por almacén, y ninguno escribe en el del otro** (ADR-0026, «Quién sincroniza», decisión de la dirección al cerrar la 012). La consola sincroniza **su carpeta**, bajo el cerrojo de la 012; la web sincroniza **su IndexedDB**, en transacciones. La web **no escribe en la carpeta** (ADR-0019, enmienda; test «never writes in a folder of the disk from the browser»), y eso no cambia. El único punto con varios escritores es el remoto, donde la escritura condicional es atómica en el servidor; en esta feature, en el remoto simulado.

**El puerto de hoy no puede dar réplicas idénticas.** `append` y `replace` reciben `LedgerEvent[]` y los vuelven a serializar con `encodeLine` (`packages/domain/src/ports/ledger-store.ts`; `packages/adapters/src/ledger-store/{memory,file,blob}.ts`). Por eso ADR-0026 (Parte A, enmienda) añade **dos operaciones sobre líneas crudas** —añadir y reemplazar con archivo— que escriben **los bytes tal cual**, y **la sincronización y la restauración usan solo esas**. Es el bloque 1, y va antes que el motor.

**La sincronización es explícita** (ADR-0026, Parte B, enmienda): la inicia el usuario. Nada la dispara al arrancar, al registrar, con un temporizador ni al recuperar la conexión. En esta feature no hay botón ni orden (son de la 015), así que la regla se ata con un test de estructura: **el motor no es alcanzable desde el arranque de la web ni desde ningún caso de uso que escriba**.

## 1. Lee antes de hacer nada, en este orden

1. `CLAUDE.md` entero. Te afectan de lleno las *domain traps* **7** (el libro propio es la fuente de verdad), **8** (append-only; anular lo consumido se rechaza) y **10** (el cargador rechaza versiones más nuevas; `append` nunca reserializa). Y los principios: *every derived value must be recomputable*, *fail safe*, *20-year survival*.
2. `.specify/memory/constitution.md` **1.6.1**: **I** (el libro es la fuente de verdad), **II** (append-only), **V** (fallo seguro) y «Restricciones técnicas → Seguridad» (validación siempre en el backend).
3. **Las ADRs, enteras y con sus enmiendas**:
   - **ADR-0026 entera**, con sus **cuatro enmiendas** y sus **dos notas** (la del cierre de la 013 y la de ADR-0033, del 2026-09-25). Es tu encargo. Léela dos veces: la redacción tachada dice qué se descartó y por qué, y varias reglas solo se entienden con el caso que las motivó (los once casos de «Contexto»).
   - **ADR-0032** (restauración, pasos 5 y 6: líneas crudas y lo retenido tras restaurar), **ADR-0033** (el dispositivo sale de la credencial, punto 6; lo demás es de la 015), **ADR-0003** (anulación y corrección), **ADR-0006** (orden canónico = posición en el fichero; `If-Match`), **ADR-0012** (huella repetida: aviso con confirmación), **ADR-0015** (las mutaciones exigen un libro válido), **ADR-0018** con su enmienda (fotos completas), **ADR-0019** con su enmienda (la web en IndexedDB), **ADR-0020** y **ADR-0025** (presentaciones y renuncias: lo que sella el prefijo), **ADR-0022** (fotos completas).
   - ADR-0027 y ADR-0028 solo para saber dónde acaba tu encargo.
4. **`docs/api.md` entero.** Es el contrato que cumple el remoto simulado (§5) y el que la 015 implementará sobre HTTP. Si al construir el motor encuentras que el contrato no alcanza, **no lo cambies**: `questions.md`.
5. `docs/decision-roadmap.md`, **Ronda 8** entera: el plan por etapas (qué es de la 014, qué de la 015, 016 y 017) y la regla de lo **SIN VERIFICAR**.
6. `docs/data-schema.md` §1 (filas de `sync/state.json`, `sync/held.jsonl`, `sync/discarded.jsonl`, `sync/devices/`, `archive/` y `ledger.lock`), **§5 entero** (el contrato de `compact`, punto (6) —«previsto para la feature de sincronización; el código actual todavía no lo comprueba»—, las operaciones de líneas crudas, el cerrojo y la transacción única de IndexedDB) y §6.3 (anulación, corrección, raíz y una sola corrección viva).
7. `specs/012-ecb-reference-rates/questions.md` §1 (la File System Access API no crea en exclusiva), §8 (D1), §11.1, §12.1 y §13; y `specs/013-daily-close-prices/questions.md` §10 (los gemelos de `tsc -b`). De ahí salen las lecciones de §2 ter.
8. `docs/prompts/013-daily-close-prices.md`, §2 bis, §2 ter y §5: las reglas de operación y la disciplina de mutación siguen valiendo enteras.
9. **El código. Todo lo que este prompt afirma de él está comprobado sobre `develop` (`523abb8`) el 2026-09-25; compruébalo tú otra vez antes de tocarlo.**
   - **El puerto**: `packages/domain/src/ports/ledger-store.ts`. `LoadedLedger` ya trae `lines` (las líneas crudas, en orden de fichero, sin salto final) además de `events` y `etag`. `append(events, etag)` y `replace(events, etag, archiveName)` reserializan.
   - **Los adaptadores**: `packages/adapters/src/ledger-store/memory.ts` (**su etag es un contador de versión, no el SHA-256 de los bytes**: el hash del prefijo sincronizado no puede salir del etag del puerto, se calcula de los bytes), `file.ts` (etag = SHA-256; toda escritura bajo `withFolderLock`, temporal, `sync`, `assertOwned`, renombrado; `beforeCommit` como costura de prueba), `blob.ts` (`LedgerBlob.update`: leer, comparar y escribir en **un** paso atómico del medio) y `browser/indexeddb.ts` (`BrowserLedgerBlob`, `transact`, una transacción de lectura y escritura sobre `LEDGER_STORE`). **No hay adaptador de S3**: es de la 015.
   - **El cerrojo**: `packages/adapters/src/ledger-store/folder-lock.ts`. Cubre **toda** escritura en la carpeta, `sync/` incluida (su comentario de cabecera lo dice, y dice que las operaciones de líneas crudas «take it as they are»). **No es reentrante**: `withFolderLock` → `acquireFolderLock` → `open(…, "wx")`, y un segundo intento del mismo proceso falla con `LedgerLockedError` contra sí mismo. `FileLedgerStore.append` y `replace` toman el cerrojo **dentro**: un cliente que tome el cerrojo para escribir `held.jsonl` y después llame a una operación del puerto se bloquea a sí mismo. El plan dice cómo se escribe la secuencia de §3, bloque 4, **bajo una sola toma** del cerrojo.
   - **IndexedDB**: `packages/adapters/src/ledger-store/browser/idb.ts` (`DB_VERSION = 2`; el `upgradeneeded` crea `ledger`, `handles` y `drafts` por nombre). `openAtlasDb` está en el camino del arranque (`apps/web/src/ledger/store.ts` → `@atlas/adapters/browser`): compruébalo con los mapas de fuente, porque cualquier byte que añadas ahí es del arranque (§5).
   - **Los tests de contrato**: `packages/adapters/test/ledger-store.contract.ts`, que corren `memory.test.ts`, `file.test.ts`, `blob.test.ts` y `browser.test.ts` (con `fake-idb.ts`).
   - **Registrar, corregir y anular**: `packages/domain/src/usecases/record-event.ts` (`checkInvalid`, la huella repetida con `DuplicateFingerprintError` y `confirmDuplicate`, `acceptInvalid` solo para `settings_changed`, `closedYearsTouched`), `rectify.ts` (**`correctEvent` añade `[reversal, event]` en una sola escritura**, línea 175), `packages/domain/src/ecb/rule-change.ts` (**`writeRateCorrections` añade una cadena de varias parejas en una sola escritura**, comprobada entera) y `packages/domain/src/projections/project-ledger.ts` (`dangling_correction`, `second_live_correction` por raíz, `already_reversed`) con `correction-root.ts`.
   - **Lo que sella el prefijo**: `tax_return_filed` (con `ledger_fingerprint`) y `filing_fingerprint_waived` en `packages/domain/src/schema/events.ts`. **La huella de una presentación no se comprueba al registrarla**: `validate.ts` solo valida su forma; la comprueban `projections/deep-check.ts` y `usecases/compact.ts` (`checkFilingFingerprints`), y `compact` **vuelve a sellar** tras reescribir (`resealFilings`).
   - **`compact`**: `packages/domain/src/usecases/compact.ts` (`planCompact`, `compactLedger`) y `apps/cli/src/commands/compact.ts`. Hoy no mira `sync/`.
   - **Lo que reescribe el libro de la web**: `packages/adapters/src/ledger-store/browser/transfer.ts`, `replaceLedgerText`, llamado desde `apps/web/src/ledger/export.ts` (`importLedger`). **Sustituye el libro entero de IndexedDB por un fichero importado.** En una web sincronizada, eso borraría sus pendientes y dejaría el marcador sin sentido: ADR-0026 no lo contempla (§6.2 P2).
   - **La copia de la consola**: `apps/cli/src/commands/backup.ts` copia **solo `ledger.jsonl`**. Lo retenido sale del libro hacia `sync/held.jsonl`, así que `atlas backup` no lo copiaría (§6.2 P3).
   - **Los guardianes**: `tests/architecture.test.ts` —«never writes in a folder of the disk from the browser» (línea 1067), «keeps out every file that is the compiled twin of a source» (línea 1649; mira **el índice de git**, no el árbol de trabajo), las puertas fuera del barril (`@atlas/domain/ecb`, `@atlas/domain/quotes`) y los de alcance de la 013— y `tests/messages.test.ts`.
   - **El paquete web**: `apps/web/scripts/check-bundle.mjs` (`BOOT_BUDGET_GZIP_BYTES`, `TOTAL_BUDGET_GZIP_BYTES`, `LAZY_ONLY`) y `packages/adapters/package.json` (`exports`).

Si algo es ambiguo, contradictorio o te bloquea, **no lo resuelvas**: `specs/014-ledger-sync-core/questions.md` y avisa. Nada fiscal ni estructural se decide aquí.

## 2. Flujo de trabajo

1. Worktree separado, rama desde `develop` **actualizado**:
   ```bash
   cd ~/projects/atlas-portfolio-tracker && git fetch origin && git worktree add ../atlas-portfolio-tracker-014 -b feature/014-ledger-sync-core origin/develop
   cd ../atlas-portfolio-tracker-014 && git config core.hooksPath .githooks && nvm use && npm ci
   ```
2. Spec Kit en `specs/014-ledger-sync-core/` (español, identificadores en inglés). **Para después de `spec.md` y `plan.md`**, con tus preguntas en `questions.md`, y espera el visto bueno antes de escribir código: **quien contesta es la dirección**. Tienen que llegar con ese alto, no dentro del primer commit:
   - **las verificaciones del bloque 0**, cada una con su fuente, su fecha y lo que dice;
   - **la tabla de reglas de §3, bloque 3**, una fila por regla de ADR-0026, con el test que la ata y el mutante que la rompe (§5): es el índice de la feature, y la dirección la revisa antes que nada;
   - **los formatos, escritos como contrato**: `sync/state.json` (el marcador: cuántas líneas, el hash de sus bytes y lo que haga falta para reconstruirlo), `sync/held.jsonl` y `sync/discarded.jsonl` (la línea tal cual **más** su motivo, y cómo se separa lo uno de lo otro sin tocar los bytes de la línea), su equivalente en IndexedDB, el nombre del archivo que deja una sincronización que reordena (`docs/data-schema.md` §1 dice que existe y no lo nombra) y el de `sync/devices/<dispositivo>.json` en el remoto simulado (`docs/api.md` §5.3);
   - **el orden de las escrituras locales** del paso 6 —`held.jsonl`, libro, marcador— con **qué pasa si el proceso muere entre cada dos**, y el test que lo prueba en cada hueco (§3, bloque 4);
   - **qué avisos «piden confirmación»** a efectos de la regla del caso 5, **enumerados leyendo el código** (la huella repetida, el ejercicio cerrado de ADR-0020, lo que invalida un `settings_changed` con `acceptInvalid`… y los que encuentres), cada uno con dónde se emite: la dirección confirma la lista;
   - **dónde se recuerda una confirmación** dada al resolver una línea retenida (un duplicado confirmado) hasta que la línea sube con `confirm_duplicate`, porque la línea del libro no la guarda;
   - **la línea de partida del paquete web** medida con `npm run build` sobre tu `develop`, y **tu estimación trozo a trozo** de lo que añade esta feature al arranque y al total (§5).
3. Implementación por bloques, en el orden de §3, commits atómicos, Conventional Commits en inglés.
4. **Entrega: abre tú la PR a `develop`**, con la plantilla del repositorio y su lista de comprobación **rellenada con honestidad** (`CLAUDE.md`, regla 4). **No la fusiones nunca**: revisa y fusiona la dirección.

## 2 bis. Reglas de operación

- **`packages/domain` al 100 % de líneas y ramas.** Bloqueante. Una rama muerta **se borra**, con un comentario que explique el invariante.
- **Ninguna dependencia nueva**, ni para el remoto simulado. Para el navegador, el Chromium de Playwright de `~/.cache/ms-playwright/`, conducido **desde tu scratchpad**; nunca Playwright en un `package.json`.
- **Ninguna petición de red.** Nada de `fetch`, ni de un servidor local, ni siquiera en los tests: el remoto simulado es un objeto en memoria o un directorio, detrás de un puerto (§3, bloque 5). El cliente HTTP es de la 015.
- **Toda escritura en la carpeta del libro, `sync/` incluida, bajo el cerrojo** (ADR-0026, Parte B), **y comprobar y escribir dentro de la misma toma**. **Nada del remoto con el cerrojo tomado**: se descarga fuera, y se **compara y escribe** dentro. En esta feature el remoto es local y rápido; en la 015 será la red, y el diseño no puede cambiar por eso.
- **Toda lectura seguida de escritura en IndexedDB, en una sola transacción**, como `LedgerBlob.update` (feature 012, bloque 0). Nada asíncrono que no sea IndexedDB entre la lectura y la escritura.
- **Las líneas se comparan por sus bytes exactos**, nunca por `id`, por huella ni por un hash de la línea reserializada (§2 ter).
- **No toques `docs/`, `.githooks/`, `.claude/`, `.specify/` ni `CLAUDE.md`**, con una sola excepción: **proponer una ADR con `/adr`**, en estado `Propuesta`. Lo que creas que debe cambiar en ellos —en `docs/api.md` también— va a `questions.md`, apartado «Documentos».
- **No toques el esquema del libro.** Esta feature **no añade ningún tipo de evento ni ningún campo** y no sube `schema_version`. El estado de la sincronización vive **fuera del libro** (`sync/`, y su equivalente en IndexedDB). Si algo parece pedir un campo en una línea, **para**.
- **Ninguna regla fuera del dominio.** Qué es una pareja, qué sella el prefijo, qué es una foto concurrente, cuándo se para, qué se retiene y con qué motivo, y cuándo se niega `compact` lo decide `packages/domain`; los clientes solo leen, escriben y ordenan las escrituras (ADR-0007).
- **Cada motivo con su literal, en su propia llamada**: el motivo de una retención, el de un rechazo del remoto y el de la negativa de `compact`. Ninguno se pliega en otro (§2 ter, el ternario de la 011).
- **Las fechas que escribas en cualquier documento van en `Europe/Madrid`**, no en UTC.
- **Verifica antes de tocar lo que este prompt afirma del código.** Dos de los prompts anteriores llevaron premisas falsas que costaron rondas enteras. Si algo de aquí no cuadra con lo que ves, **para y dilo**: encontrar un error de este prompt es trabajo hecho.
- **Commits sin rastro de IA**, ni en el asunto ni en el cuerpo. **`npm run lint` verde antes de cada commit** y otra vez como último paso. **Empuja tu rama a menudo** (§2 ter) —tu rama y solo tu rama— y **nunca fusiones nada**.

## 2 ter. Lo que las rondas anteriores aprendieron a golpes

Siguen valiendo enteras las lecciones de §2 ter de los prompts 012 y 013: **un test que no has visto fallar no es un test**; **un mutante que sobrevive puede ser un mutante que nunca se aplicó** (el guion de mutación afirma que la sustitución ocurre, restaura y compara el fichero); **un resultado leído a través de una tubería se come el error** (redirige a un fichero y lee `$?`); **mirar la pantalla encuentra lo que ningún test encuentra**; **un ternario que colapsa un conjunto en otro más pequeño esconde un caso**; **el compilador no enumera a las consumidoras de un tipo que solo se transporta**; y **una verificación que no se escribe con su fuente no es una verificación**.

Y seis que costaron las rondas de la 012 y la 013, todas de aplicación directa aquí:

- **Un arreglo abre defectos nuevos, así que después de cada uno se mira alrededor.** En la 012, dos de cuatro arreglos abrieron un defecto nuevo (`specs/012-ecb-reference-rates/questions.md` §13): el de la segunda pasada nació de la decisión tomada al arreglar la primera. La sincronización es una máquina de estados con cortes posibles entre cada escritura: **después de cada arreglo** se vuelve a mirar quién más llama a lo que has cambiado, qué hueco de corte abre, y **se repiten enteros** los lotes de mutación afectados.
- **Comprobar y escribir van siempre juntos**, bajo el cerrojo o en una transacción. En la 012, dos confirmaciones a la vez resucitaron un borrador porque el cerrojo cubría cada escritura y no la secuencia (§12.1). Aquí la secuencia es el paso 6: comparar el etag leído en el paso 1, escribir `held.jsonl`, reescribir el libro y actualizar el marcador. **Si el libro local cambió entre el paso 1 y el 6** (la consola añadió una línea en otra terminal, la web registró en otra pestaña), no se escribe nada y se vuelve a empezar. Y el cerrojo **no es reentrante** (§1.9).
- **La identidad exacta vale más que una huella.** En la 012, dar un borrador por registrado porque el libro tenía un evento con su misma huella **borró la única copia de una operación** (§11.1). Aquí se decide muchas veces si algo «ya está»: si el remoto ya tiene una pendiente (paso 2), si una línea subida está dentro (paso 5), si una línea del libro ya está en `held.jsonl` (el corte entre escribir lo retenido y quitarlo del libro). **Se decide por los bytes exactos de la línea.** Dos líneas con el mismo `id` y bytes distintos **no** son la misma: la segunda la rechaza el dominio (`duplicate_id`) y queda retenida, nunca se da por subida.
- **Lo verificado en la plataforma real no se sustituye por un ensayo.** El remoto simulado prueba **tu lógica**, no S3: que `PutObject` con `If-Match` es atómico, qué devuelve en un conflicto y cómo se traduce el etag es de la 015, que lo verifica contra la documentación y contra AWS. Lo que **sí** es de esta feature se prueba en la plataforma real: la transacción de IndexedDB y su durabilidad **en Chromium de verdad** (no solo en `fake-idb.ts`), y el `fsync` y el renombrado **en el sistema de ficheros del usuario** (WSL2). Bloque 0.
- **Sube la rama a menudo, porque las sesiones se caen.** Una sesión cortada a media feature sin la rama empujada es trabajo perdido. Empuja cada pocos commits, siempre en verde.
- **Un `tsc -b` puede emitir gemelos `.js` junto a las fuentes, y los tests los leen en su lugar.** En la 013, importar el dominio desde el test de arquitectura hizo que `tsc -b` compilara **gemelos `.js` junto a 18 fuentes del dominio**; git los ignoraba, y Vite y vitest los usaron en lugar de los `.ts` (`specs/013-daily-close-prices/questions.md` §10). El test «keeps out every file that is the compiled twin of a source» **mira el índice de git, no el árbol de trabajo**, así que no los ve. **Antes de dar por buena una suite verde** —y antes de cada lote de mutación—, busca en el árbol de trabajo un `.js` junto a un `.ts` del mismo nombre fuera de `dist*/`, y si hay alguno, bórralo y repite. Un test que importa código de otro paquete lo importa **por su nombre de paquete**, nunca por ruta relativa a `src/`.

## 3. Alcance, por bloques y en este orden

El orden tiene motivo: **primero se verifica** lo que la ADR da por cierto (0); después **el puerto**, que es la base de las réplicas idénticas (1); después **los guardianes**, antes de que exista un solo módulo de sincronización que pueda saltárselos (2); después **el núcleo puro** (3); después **los dos clientes** con sus almacenes de estado (4); después **el remoto simulado** y los recorridos completos (5); y **al final, `compact`** y lo que se niega sobre un libro sincronizado (6).

### Bloque 0 — Verificar antes de escribir código

La regla de la Ronda 8: **cada feature verifica, con fuente, antes de escribir código** lo que da por cierto, y lo que encuentra se escribe en `questions.md` con su fecha. ADR-0026 no deja SIN VERIFICAR nada de esta feature, pero el diseño descansa en cuatro hechos de plataforma que nadie ha comprobado:

1. **La durabilidad de una transacción de IndexedDB.** ADR-0026 exige que `held.jsonl` «se asegure en disco» antes de quitar la línea del libro. En IndexedDB, ¿un `complete` significa que está en disco? Lee la especificación (W3C *Indexed Database API 3.0*, `durability`: `"default"`, `"strict"`, `"relaxed"`) y **qué hace cada navegador por defecto** (Chromium, Firefox, Safari), con fuente. Si el valor por defecto no asegura, el plan dice qué `durability` pide cada transacción de la sincronización y **se prueba en Chromium de verdad** que se acepta.
2. **Una transacción sobre dos almacenes de la misma base es atómica** —las dos escrituras o ninguna— en Chromium de verdad, abortándola a mitad con el navegador conducido desde el scratchpad. Y **una transacción no puede abarcar dos bases distintas**: si el estado de la sincronización fuera a otra base, lo retenido y el libro irían en dos transacciones ordenadas. Cuál de las dos formas se elige depende también del arranque (§5): escríbelo con la medida.
3. **`fsync` y `rename` en WSL2**, sobre el sistema de ficheros donde vive el libro del usuario (ext4 de la distribución, no `/mnt/c`): que `FileHandle.sync()` seguido de `rename` deja el fichero nuevo entero o el viejo entero tras matar el proceso entre los dos (`kill -9` desde un guion en el scratchpad, cien veces). Si el libro pudiera vivir en `/mnt/c` (9P/DrvFs), dilo con lo que encuentres y **para**: no se afirma lo que no se ha probado.
4. **Los bytes de una línea sobreviven al viaje que hará en la 015**: una línea del libro metida como cadena en un cuerpo JSON (`docs/api.md` §5.2), serializada con `JSON.stringify` y leída con `JSON.parse`, **da los mismos bytes UTF-8** que la original, con caracteres fuera del ASCII (`ñ`, tildes, un emoji en `notes`), barras invertidas y comillas escapadas en el texto de la línea. Una prueba con `fast-check` sobre líneas generadas por el sintético basta; su resultado va al plan.

**Qué se hace si sale mal:** si IndexedDB no puede asegurar en disco, o si la atomicidad de dos almacenes no se sostiene en Chromium, **para** y dilo con la prueba: es la garantía de «nunca en ninguno» de ADR-0026 y no se entrega con un aviso. Si falla el punto 4, **para**: el contrato de `docs/api.md` §5.2 lo decide la dirección.

### Bloque 1 — Las operaciones de líneas crudas del puerto (ADR-0026, Parte A, enmienda; `docs/data-schema.md` §5)

- **Dos operaciones nuevas en `LedgerStore`**: añadir líneas crudas al final, y reemplazar el contenido por líneas crudas archivando antes el original. Nombres en el plan. **Escriben los bytes tal cual**: cada línea recibida + `"\n"`, sin `decodeLine`/`encodeLine` de por medio para escribir. **Con los mismos etag, archivo y rechazos** que `append` y `replace`: `ConflictError` con un etag viejo, `ArchiveExistsError` con un archivo que ya existe, nada escrito en ninguno de los dos casos, nombre de archivo sin `/` ni `\`.
- **Validan antes de escribir lo que el cargador exigiría al leer**: cada línea se decodifica con el esquema del almacén, y una línea de una versión más nueva, ilegible o con un salto de línea dentro **se rechaza sin escribir nada**. Un almacén que acepta bytes que luego no sabe cargar deja el libro ilegible. Qué error y con qué código, en el plan.
- **Las cumplen los tres adaptadores que existen** —memoria, fichero (bajo el cerrojo, con temporal, `sync`, `assertOwned` y renombrado) y `BlobLedgerStore` (por `LedgerBlob.update`, una sola transacción en el navegador)— y **pasan los mismos tests de contrato**, extendidos en `ledger-store.contract.ts`. El de S3 los pasará en la 015 (§6.1 (b)).
- **El test que las distingue de `append`**: una línea **no canónica** —claves en otro orden, `"101.50"` donde `encodeLine` escribiría otra cosa, espacios— se escribe y se relee **idéntica byte a byte**. Con `append`, la misma línea saldría reserializada. Ese es el mutante 1 de §5.

### Bloque 2 — Los guardianes, antes que el motor

Antes de crear el núcleo, **los tests de arquitectura tienen que cubrir lo que vas a crear**. Decide en el plan dónde vive el dominio de la sincronización (una carpeta propia, por ejemplo `packages/domain/src/sync/`, detrás de **una puerta propia** como `@atlas/domain/sync`, **nunca** en el barril `index.ts`, que está en el arranque de la web), y entonces:

- **Nada fiscal alcanza la sincronización**: desde `tax/`, `informative/` y `project-ledger.ts`, a cualquier profundidad, no se alcanza ningún módulo de `sync/`, **leído del grafo o de la carpeta, no de una lista escrita a mano**. Y al revés: la sincronización no importa nada de `tax/` ni de `informative/`.
- **La sincronización es explícita**: ningún caso de uso que escriba (`record-event`, `rectify`, `compact`, `rule-change`, la confirmación de borradores) y ningún módulo del arranque de la web alcanza el motor. Y en los módulos de sincronización no aparece `setInterval`, `setTimeout`, `addEventListener("online"…)` ni `visibilitychange`.
- **Un cliente por almacén**: el cliente de la web solo alcanza el almacén de IndexedDB, y el de la consola solo el de fichero y el cerrojo. El test «never writes in a folder of the disk from the browser» **sigue en verde sin tocarlo**, y el cliente de la web cae dentro de lo que mira: compruébalo.
- **Una puerta fuera del barril**, con su test, como «keeps the ECB and the local configuration out of index.ts».
- **Nada de la sincronización en el arranque**: los módulos nuevos van en `LAZY_ONLY` de `check-bundle.mjs` **desde su primer commit** (§5).
- **Míralos fallar antes de seguir**: con el guardián puesto y un módulo de sincronización vacío, importarlo desde `tax/`, desde `record-event.ts` y desde el arranque de la web tiene que ponerlo rojo. Son mutantes de §5.

### Bloque 3 — El núcleo puro (ADR-0026, Parte B)

Funciones **puras** del dominio, al 100 %, sin E/S. **Un solo caso de uso** —reaplicar una cola de líneas sobre un libro y clasificar el resultado— **que usan el cliente y el remoto** (ADR-0026, Consecuencias: «que usan el cliente y la Lambda»): el cliente para decidir qué sube y qué retiene, el remoto simulado (y la Lambda de la 015) para decidir qué acepta. Las reglas que solo son de uno de los dos lados se dicen como tales. **Cada regla, con su test y su mutante nombrado en §5**:

| Regla (ADR-0026) | Lado | Qué se construye |
|---|---|---|
| **Cola por dispositivo.** Las pendientes son las líneas posteriores al prefijo sincronizado del marcador | Cliente | Separar prefijo y cola a partir de las líneas crudas y del marcador (cuántas líneas y hash de sus bytes) |
| **Rescritura detectada por el hash del prefijo.** Si los bytes de las primeras `n` líneas del remoto no dan el hash del marcador, el remoto se ha reescrito: **no se sube nada**, se avisa, y solo se vuelve a descargar cuando el usuario lo pide. Al volver a descargar se retiene **todo lo que el dispositivo tenía y el remoto nuevo no**, pendiente o sincronizado (Parte A, tercera enmienda; ADR-0032 paso 6) | Cliente | La comprobación y la clasificación de lo que se retiene al volver a descargar. **Por bytes, nunca por identificadores**: una compactación que conserva los `id` y cambia el contenido **tiene** que detectarse |
| **Lo que el remoto ya tiene sale de la cola** (paso 2): una pendiente cuyos bytes exactos están en la cola del remoto | Cliente | Por bytes exactos |
| **Revalidación línea a línea, en orden local, encima del remoto** (paso 3), con la misma validación que registrar | Los dos | Proyectar con la línea añadida y rechazar lo que el dominio rechace (casos 2 y 3) |
| **Un aviso nuevo que pide confirmación** y no tenía al registrarse (caso 5) | Cliente | Comparar los avisos de la línea sobre su base local con los de la línea sobre el remoto; la lista de avisos, la del plan (§2) |
| **Foto concurrente** (caso 4): un `settings_changed` pendiente si el remoto ganó cualquier `settings_changed` desde el prefijo; un `account_updated` o `asset_updated` pendiente si el remoto cambió **esa** cuenta o **ese** activo | Cliente | Detección por tipo e identificador |
| **Lo que sella el prefijo no se mueve** (caso 9): `tax_return_filed` y `filing_fingerprint_waived` nunca se reaplican solas; se retienen si el remoto ganó líneas o si una pendiente anterior quedó retenida, y se propone registrarlas otra vez sobre el libro actual | Cliente | **Una lista cerrada de los tipos que sellan**, y un test que **recorre los 26 tipos del catálogo** y exige que cada uno esté clasificado a mano como que sella o que no: un tipo nuevo lo pone rojo hasta que alguien decida |
| **La pareja de anulación y corrección es una unidad, en los dos sentidos** (casos 10 y 11; segunda enmienda): la anulación de una operación y la línea con `corrects_id` a ella que la acompaña en la cola. Si falla cualquiera, se para **antes de la anulación** y se retienen las dos juntas; **cuenta como una sola línea** para la regla de parada | Los dos | La detección de la pareja en la cola, la evaluación conjunta y, en el remoto, la declaración `has_correction` y `pair_incomplete` (`docs/api.md` §5.2). Si una cadena de varias parejas (`writeRateCorrections`) es una unidad o varias: **[PENDIENTE] P1** |
| **Parar en la primera que falla** (enmienda): las anteriores se suben; la que falla queda retenida con su motivo visible | Los dos | En el remoto, el tramo válido hasta la primera rechazada (`docs/api.md` §5.2) |
| **La cola no avanza detrás de algo retenido** (caso 11, tercera enmienda): mientras haya una línea o pareja retenida sin resolver, **no se sube nada de lo que va detrás**, aunque se pudiera reaplicar; se queda **pendiente** (no retenida) hasta que el usuario resuelva | Cliente | También en la sincronización **siguiente**: lo retenido de antes bloquea igual |
| **Rechazos del remoto** (paso 4): un rechazo que no es conflicto (el reloj, una versión que no conoce, una huella sin confirmar) pasa a retenida con el motivo del remoto | Cliente | Cada código de `docs/api.md` §7 con su motivo, sin plegar |
| **Versiones** (caso 6): un cliente que no sabe cargar el remoto no sube nada; sus pendientes esperan | Cliente | El error del cargador, dicho como tal |
| **El primer dispositivo y el que se une** (casos límite): con el remoto vacío, el libro entero sube; uno que se une con libro propio **elige** entre empezar desde el remoto o subir sus líneas como pendientes, que pasan por la revalidación | Cliente | Dos formas de iniciar, las dos explícitas |
| **Resolver lo retenido**: confirmar (duplicado, ejercicio cerrado), rehacer (una foto concurrente o una línea que sella se rehacen sobre el estado actual) o descartar, que es explícito y deja la línea en `sync/discarded.jsonl`. **Descartar una anulación retiene también su corrección** | Cliente | Los casos de uso puros de las tres resoluciones. Qué pasa con la línea retenida cuando se rehace (sale a `discarded` con un motivo propio, u otra cosa): propuesta en el plan |

Y la regla general de ADR-0026 que las envuelve: **el remoto nunca recibe un libro inválido**, y **lo retenido nunca se sube solo**.

### Bloque 4 — Los dos clientes y sus almacenes de estado (ADR-0026, Parte B, pasos 1 a 7)

Cada cliente es la orquestación de los siete pasos sobre **su** almacén y un **puerto del remoto** (§3, bloque 5), en los adaptadores o en la aplicación, nunca en el dominio (que no hace E/S). **Ninguno tiene interfaz en esta feature**: ni botón, ni orden, ni pantalla (§6.1 (a)).

- **El estado del dispositivo, fuera del libro**: el marcador (`sync/state.json`), lo retenido (`sync/held.jsonl`) y lo descartado (`sync/discarded.jsonl`) en la carpeta de la consola, y su equivalente en IndexedDB para la web. **El marcador no es una simple caché** (segunda enmienda): ilegible, `compact` se niega (bloque 6); para sincronizar, en cambio, se puede reconstruir: la parte común con el remoto es lo sincronizado.
- **Consola**: toda escritura en `sync/` y en el libro bajo el cerrojo de la carpeta, **en una sola toma** para la secuencia del paso 6 (el cerrojo no es reentrante, §1.9), con escritura atómica (temporal `"wx"`, `sync`, `assertOwned`, renombrado) como `FileEcbHistoryStore`. **Nada del remoto con el cerrojo tomado.**
- **Web**: la secuencia del paso 6 en **una transacción** si el estado vive en la misma base que el libro (lo que exige subir `DB_VERSION` a 3 y añadir un almacén en el `upgradeneeded`, que está en el arranque: §5), o en **dos transacciones ordenadas** —lo retenido primero, asegurado en disco (bloque 0, punto 1)— si vive en otra. El plan elige con la medida. Si otra pestaña con una versión anterior bloquea la subida de versión, se dice así, como hoy, no «el navegador no guarda datos».
- **El orden de las escrituras locales del paso 6** es el del plan (§2), y **cada hueco entre dos escrituras tiene su test de corte**: con la costura `beforeCommit` del almacén de fichero, o una equivalente, se mata la operación en cada hueco y se comprueba que **ninguna línea queda en ningún sitio menos de una vez** y que la sincronización siguiente lo reconoce **por bytes exactos** (una línea que está a la vez en el libro y en `held.jsonl` no se reaplica: sale del libro).
- **El paso 6 exige el etag leído en el paso 1**: si el libro local cambió mientras tanto, no se escribe nada y se vuelve a empezar. Con líneas que cambian de sitio, **se archivan antes los bytes locales anteriores** (`docs/data-schema.md` §1, `archive/`) con la operación de reemplazo de líneas crudas; si solo llegó cola del remoto, es un añadido puro.
- **Paso 5: una línea sale de la cola solo después de releer el remoto y encontrarla dentro, byte a byte.** Nunca por la respuesta de la subida. La **respuesta perdida** tiene su test: el remoto acepta y la respuesta no llega; la sincronización siguiente quita esas líneas en el paso 2 y no las duplica.
- **Paso 7: publicar el estado de la cola** (pendientes y retenidas, y cuándo sincronizó) en el remoto simulado, **con el dispositivo que da el remoto, nunca uno que el cliente ponga en el cuerpo** (`docs/api.md` §2.3 y §5.3; ADR-0033, punto 6).

### Bloque 5 — El remoto simulado y los recorridos completos

- **Un puerto del remoto** en el dominio —leer el libro remoto (sus bytes y su etag), añadir líneas con una condición y publicar el estado de la cola—, con la **semántica de `docs/api.md` §5**: etag = SHA-256 de los bytes, `If-Match` obligatorio, **`412` sin escribir nada** si no cuadra, el tramo válido hasta la primera línea rechazada con su motivo, `confirm_duplicate` por línea, `has_correction` y `pair_incomplete`, la tolerancia del reloj como parámetro, y las versiones que no conoce. Nombre en el plan.
- **Dos implementaciones del remoto simulado**, en `packages/adapters` o en los tests (el plan dice dónde y por qué): **en memoria**, y **en un directorio** que imita el bucket (`ledger/ledger.jsonl`, `sync/devices/`), para poder probar **dos clientes de verdad** —una consola y una web con `fake-idb`, o dos consolas en dos carpetas— contra el mismo remoto. Las dos se construyen sobre una operación de líneas crudas y **usan el mismo caso de uso del bloque 3** para aceptar, como hará la Lambda. **Ninguna se presenta como un adaptador de producción**: su nombre y su comentario lo dicen.
- **Costuras de prueba** para lo que el remoto real hará y el simulado tiene que poder hacer a propósito: otro escritor que gana la carrera (`412`), una respuesta que se pierde tras escribir, un remoto reescrito (compactado conservando los `id`; restaurado con menos líneas).
- **Los recorridos completos**, con dos dispositivos y el mismo remoto, **uno por cada uno de los once casos de ADR-0026** («Contexto»), escritos como el caso lo cuenta: el duplicado (1), el consumo cruzado (2), la doble rectificación (3), las fotos concurrentes (4), el aviso nuevo (5), las versiones (6), el remoto reescrito (7), el reloj (8), la presentación registrada sin conexión (9), la corrección que ya no cabe (10) y la venta sobre otra base (11). **Cada uno acaba comprobando** que el remoto es válido, que cada réplica es **idéntica byte a byte** al remoto seguido de su cola, y que **ninguna línea escrita en ningún dispositivo ha desaparecido**: o está en el remoto, o en una cola, o en `held.jsonl`, o en `discarded.jsonl` por decisión explícita.
- **Una propiedad con `fast-check`**, además de los once: dos o tres dispositivos que registran, corrigen y anulan al azar sobre un libro sintético y sincronizan en orden aleatorio, con cortes aleatorios en el paso 6. Al final: el remoto carga y es válido, las réplicas son idénticas, **y el conjunto de todas las líneas escritas es igual a la unión de remoto, colas, retenidas y descartadas**. Es el test de «ninguna línea se pierde».
- **Ningún número fiscal cambia por sincronizar**: el mismo libro —los mismos bytes— **con y sin** `sync/` al lado, cargado por la consola, da **la misma salida** de `tax`, `gains`, `income`, `m720`, `m721` y `filed`, **byte a byte**, sobre el sintético (`synthetic-v1`) y sobre un libro que haya pasado por un recorrido con reordenación. Y el *golden* `synthetic-v1.tax.json` no se mueve. La predicción —que no se mueve nada— se escribe **antes** de correr la suite, y si se mueve algo, **se para y se pregunta**.

### Bloque 6 — `compact` y lo que se niega sobre un libro sincronizado (ADR-0026, Parte A y Parte B; ADR-0032)

- **Una función pura** que decide si se puede reescribir el remoto (compactarlo o restaurarlo) y si se puede compactar la carpeta local, a partir del estado del marcador (legible, ilegible, ausente con `sync/` presente), de la cola local y de **los estados publicados en `sync/devices/`**, y que devuelve **el motivo con su literal**: hay pendientes o retenidas en alguna cola conocida; el marcador es ilegible; existe `sync/` y falta el marcador; la carpeta está sincronizada. Nombres en el plan.
- **`atlas compact` en una carpeta sincronizada se niega** y remite a la operación de administración sobre el remoto (ADR-0026, Parte A; `docs/data-schema.md` §5, punto (6)). Con el marcador ilegible, se niega; con `sync/` y sin marcador, se niega. Un mensaje por motivo, traducido.
- **La restauración y el `compact` del remoto** son operaciones de administración con credenciales de AWS (ADR-0032), y **ninguna feature de la hoja de ruta las tiene asignadas** (§6.2 P5). Aquí solo la función pura que las niega, con sus tests, lista para que las use quien las construya.
- **Desactivar la sincronización** solo con una orden explícita (segunda enmienda). Qué hace con lo pendiente y lo retenido: **[PENDIENTE] P4**; sin respuesta, esta feature no la construye.

## 4. Fuera de alcance y bloqueado

- **La API de verdad y el inicio de sesión** (feature 015): la Lambda, el cliente HTTP de la web y el de la consola, el hash del cuerpo, `LedgerStore` sobre S3 con `If-Match`, las rutas de `docs/api.md` §3, §4 y §6, `atlas remote login` y `logout`, el registro de tokens en SSM y la pantalla de dispositivos. **Y las interfaces de la sincronización**: el botón y el contador de pendientes de la web, las órdenes de la consola para sincronizar, ver lo retenido y resolverlo, y la pantalla de lo retenido. La hoja de ruta pone «en la SPA, … la sincronización» en la 015 (§6.1 (a)).
- **Los datos de referencia por la API** (`prices/`, `reference/ecb/`): 015 y 016. Esta feature sincroniza **solo el libro**; `drafts/`, `prices/`, `reference/` y `atlas.config.json` no viajan.
- **El despliegue y todo lo de AWS** (etapa 3, features 018 y 019): **solo con el visto bueno del usuario**, y no en esta feature. Ni Terraform, ni la CLI de AWS, ni una cuenta.
- **Los importadores** (MyInvestor e IBKR, Ronda 6): bloqueados por la Fase 0, porque **faltan los ficheros reales**.
- **Las operaciones de administración** sobre el remoto (`compact` remoto, restaurar, olvidar un dispositivo): solo la función pura que las niega (bloque 6; §6.2 P5).
- **Aceptar una ADR** (puedes proponerla, con `/adr`), reabrir una aceptada, cambiar el esquema del libro o el contrato de `docs/api.md`.

## 5. Criterios de terminado

- `lint`, `typecheck`, `test:coverage`, `build` y CI en verde; `packages/domain` al **100 %** de líneas y ramas.
- **El bloque 0 escrito y reportado antes del primer commit de código**, cada verificación con su fuente, su fecha y su salida.
- **Cada regla de la tabla del bloque 3 con su test**, y lo que las preguntas de §6.2 dejen pendiente en §7, **sin implementar** y dicho en la PR.
- **Cada código nuevo de retención, rechazo y negativa, traducido en las dos interfaces**, con `tests/messages.test.ts` extendido y en verde, aunque esta feature no tenga pantallas: el código existe, y la 015 lo enseñará.
- **De cada arreglo, cómo lo viste en rojo**, escrito en `questions.md`, y **qué volviste a mirar alrededor** después (§2 ter).
- **Ningún gemelo `.js` en el árbol de trabajo** junto a una fuente, comprobado antes de cada lote de mutación y antes de abrir la PR, con la orden y su salida en `questions.md` (§2 ter).
- **Revisión por mutación**, con la disciplina de §2 ter. Escribe los tests que matan al menos estos mutantes, **cada uno visto morir**:
  1. **reserializar en una operación de líneas crudas** (`encodeLine` sobre lo recibido), en cada uno de los tres adaptadores; **aceptar una línea de una versión más nueva** o con un salto dentro; **escribir sin comparar el etag**; y **sobrescribir un archivo**;
  2. **abrir la puerta**: alcanzar un módulo de sincronización desde `tax/`, desde `informative/` o desde `project-ledger.ts`; **llamar al motor desde `record-event`** o desde el arranque de la web; un **`setInterval`** en un módulo de sincronización; y **meter la sincronización en el barril** o en el arranque (tiene que parar el *build*);
  3. **dos clientes, uno por almacén**: que el cliente de la web **escriba en la carpeta** (el test existente, sin tocarlo) o use como libro local el de la carpeta; y que el de la consola escriba fuera del cerrojo;
  4. **la cola**: tomar como pendientes las líneas desde otra posición que el prefijo del marcador; **quitar una pendiente por `id`** en vez de por bytes (una línea con el mismo `id` y otros bytes tiene que quedar retenida, no desaparecer);
  5. **la revalidación**: no reaplicar sobre el remoto (sobre la base local); **no retener** una línea que el dominio rechaza; **no retener** un aviso nuevo que pide confirmación; **retener** uno que ya tenía al registrarse;
  6. **las fotos concurrentes**: dejar pasar un `settings_changed` con otro en la cola del remoto; dejar pasar un `asset_updated` de un activo que el remoto también cambió; **retener** uno de **otro** activo;
  7. **lo que sella**: reaplicar automáticamente una `tax_return_filed` con el remoto crecido, o detrás de una retenida; **quitar un tipo de la lista cerrada**; y añadir un tipo de evento sin clasificarlo (el test que recorre los 26 tiene que ponerse rojo);
  8. **la pareja**: subir la anulación sola cuando falla la corrección (el caso 10, bloqueante en la revisión de la PR #72); subir la corrección sola cuando falla la anulación; **partir la pareja entre dos subidas**; contarla como dos líneas en la regla de parada; **descartar una anulación sin retener su corrección**; y, en el remoto, aceptar una anulación con `has_correction` sin su corrección, o aceptar la anulación cuando la corrección se rechaza;
  9. **la parada y la cola que no avanza**: seguir después de la primera que falla; **subir lo que va detrás de una retenida** en la misma sincronización o en la siguiente; y convertir en retenida lo que solo debía quedar pendiente detrás de ella;
  10. **salir de la cola**: quitar una línea con la respuesta de la subida, **sin releer el remoto**; quitar del libro local una línea retenida **antes** de asegurar `held.jsonl` (el corte en ese hueco tiene que dejarla en los dos sitios); y **perder** una línea en cualquiera de los huecos del paso 6 (la propiedad de `fast-check` tiene que encontrarlo);
  11. **el remoto reescrito**: detectarlo **por identificadores** en vez de por el hash de los bytes (una compactación que conserva los `id` y cambia el contenido tiene que detectarse); **subir algo** tras detectarlo; **volver a descargar sin que el usuario lo pida**; y, al volver a descargar, retener **solo lo pendiente** en vez de todo lo que el dispositivo tenía y el remoto nuevo no;
  12. **comprobar y escribir**: escribir el paso 6 **sin exigir el etag del paso 1** (una línea añadida entretanto se pierde); **tomar el cerrojo dos veces** en la secuencia, o escribir parte de ella fuera; en la web, **partir en dos transacciones** lo que el plan puso en una; y **llamar al remoto con el cerrojo tomado**;
  13. **el remoto simulado**: aceptar con un `If-Match` viejo; escribir algo en un `412`; escribir **detrás** de la primera línea rechazada; aceptar una huella repetida **sin** `confirm_duplicate`; y **publicar el estado de la cola con un dispositivo que venga del cuerpo**;
  14. **`compact` y la negativa**: compactar una carpeta sincronizada; compactar con el marcador ilegible; compactar con `sync/` y sin marcador; y que la función pura **permita reescribir el remoto** con pendientes o retenidas publicadas por **otro** dispositivo;
  15. **una cifra fiscal que cambie** con `sync/` presente (el test que compara byte a byte);
  16. **plegar dos motivos** de retención, de rechazo o de negativa en uno.
- **El paquete web, medido, y el arranque no tiene margen.** Sobre `develop` a 2026-09-25 (`523abb8`, `npm run build`, medido para este prompt): **arranque 75.703 bytes gzip contra un techo de 75.725 —22 bytes de margen—** y **total 276.111 contra 276.480 —369 bytes—**. En la 013, nombrar un fragmento perezoso nuevo en la entrada costó entre 7 y 9 bytes del arranque, y subir `DB_VERSION` añade al `upgradeneeded`, que es del arranque. La regla:
  - **Todo lo de la sincronización va en carga diferida**, en `LAZY_ONLY` desde su primer commit; y si el arranque **no cabe en su techo, se para** y se dice con la medida trozo a trozo. No se sube el techo del arranque, no se esconde nada con una importación dinámica que cambie lo que se ve y no se recorta una comprobación para que quepa: es una decisión de la dirección.
  - **El total** sube, si hace falta, con la regla de siempre: lo medido más un margen pequeño, **en su propio commit**, con la medida en el mensaje y en `questions.md` y la tendencia escrita en el comentario de `check-bundle.mjs`.
- **`docs/` sin cambios**, salvo una ADR nueva en estado `Propuesta` si la propones. Y `specs/014-ledger-sync-core/questions.md` con: el bloque 0 y sus fuentes, lo preguntado y lo respondido, cómo viste fallar cada test, lo medido del paquete, y **la lista de documentos que la dirección tendrá que actualizar** (como mínimo: `docs/data-schema.md` §1, con los formatos de `sync/` que queden y el nombre del archivo de una sincronización que reordena, y §5, quitando «previsto para la feature de sincronización» del punto (6) de `compact` y con los nombres de las operaciones de líneas crudas; `docs/api.md`, si el remoto simulado encontró algo que el contrato no dice; ADR-0026, con lo que el bloque 0 encontró de IndexedDB; y `docs/prompts/README.md`).
- **`npm run lint` como último paso**, redirigiendo a un fichero y leyendo `$?`. Commits sin rastro de IA, uno a uno en verde, y la rama empujada.

## 6. Decisiones

### 6.1 Fijadas por este prompt (sobre ADR-0026, ADR-0033 y la hoja de ruta)

- **(a) Sin interfaces de sincronización en la 014; el motor, entero.** La hoja de ruta pone en la 014 «el caso de uso puro de reaplicación …, el marcador, lo retenido y lo descartado, la negativa de `compact` en una carpeta sincronizada y un remoto de pruebas en memoria», y en la 015, «en la SPA, el inicio de sesión, la sincronización y el hash del cuerpo». Así que aquí van el dominio, los dos clientes sin interfaz y los recorridos contra el remoto simulado, y **los botones, las órdenes y las pantallas van a la 015**, con la API que les da sentido. *Motivo:* manda la hoja de ruta; y una orden de sincronizar que solo habla con un remoto simulado sería una funcionalidad falsa.
- **(b) Las operaciones de líneas crudas, en los tres adaptadores que existen.** La hoja de ruta dice «en sus cuatro adaptadores», pero el cuarto, S3, es de la 015 por la misma hoja de ruta («`LedgerStore` sobre S3 con `If-Match`»). La 015 lo construye pasando los mismos tests de contrato. *Motivo:* no se construye un adaptador de producción de un servicio al que esta feature no puede llamar.
- **(c) Un solo caso de uso de aceptación para el cliente y el remoto.** El remoto simulado acepta y rechaza con el mismo código del dominio que usará la Lambda, y cumple `docs/api.md` §5. *Motivo:* ADR-0026 (Consecuencias) lo pide así, y es lo que permite probar en la 014 cómo reacciona el cliente a un rechazo por línea sin esperar a la 015.
- **(d) Comparación por bytes exactos en toda decisión de «ya está».** *Motivo:* la lección de §11.1 de la 012, y que la réplica tiene que ser el remoto byte a byte (Parte A).
- **(e) Nada del remoto con el cerrojo tomado; comprobar y escribir en una sola toma.** *Motivo:* ADR-0026 Parte B, y §12.1 de la 012. En la 015 el remoto será la red, y el diseño no puede depender de que hoy sea rápido.
- **(f) El dispositivo, del remoto; nunca del cuerpo.** *Motivo:* ADR-0033, punto 6, y la nota que deja en el paso 7 de ADR-0026.

### 6.2 [PENDIENTE] — preguntas para la dirección

Cada una dice si bloquea algo. Las respuestas van a §7.

- **P1. ¿Una cadena de correcciones de tipos es una unidad o varias parejas?** `writeRateCorrections` (`packages/domain/src/ecb/rule-change.ts`) escribe **varias** parejas de anulación y corrección **en una sola escritura**, comprobadas **enteras**: si una no cabe, no se escribe ninguna. ADR-0026 define la pareja, no la cadena. Si cada pareja es una unidad, la sincronización puede subir la mitad de una cadena que el usuario confirmó entera. *Bloquea:* la regla de la pareja para cadenas (bloque 3); las parejas sueltas no.
- **P2. ¿Importar un libro en una web sincronizada se niega?** `importLedger` → `replaceLedgerText` sustituye el libro entero de IndexedDB: en una web sincronizada, borraría sus pendientes y dejaría el marcador sin sentido. ADR-0026 niega `compact` y restaurar, y no dice nada de importar. *Propuesta:* se niega con pendientes o retenidas, como `compact`, y sin ellas avisa de que desactiva la sincronización. *Bloquea:* el cambio en la importación de la web; el resto no.
- **P3. ¿`atlas backup` y la exportación de la web copian lo retenido?** Una línea retenida sale del libro local hacia `sync/held.jsonl`, y `atlas backup` copia solo `ledger.jsonl`; la exportación de la web, solo el libro. ADR-0026 acepta como riesgo que una **pendiente** viva solo en su dispositivo, no que una **retenida** quede fuera de toda copia. *Bloquea:* nada de esta feature, pero conviene decidirlo antes de que haya retenidas reales (015).
- **P4. ¿Qué hace desactivar la sincronización con lo pendiente y lo retenido?** La segunda enmienda dice que solo se desactiva con un comando explícito, no qué pasa con la cola y con `held.jsonl`. *Bloquea:* esa orden (bloque 6), que sin respuesta no se construye.
- **P5. ¿Qué feature construye el `compact` del remoto, la restauración y olvidar un dispositivo?** ADR-0026 y ADR-0032 las definen como operaciones de administración con credenciales de AWS; la hoja de ruta no las asigna (la 016 hace el **ensayo** de restauración en memoria, no la restauración). *Bloquea:* nada de la 014, que construye la función pura que las niega.
- **P6. ¿El remoto comprueba el sello de una línea que sella el prefijo?** La proyección no comprueba la huella de una presentación al registrarla (solo `check --deep` y `compact`). El cliente nunca sube una línea que sella con el remoto crecido, pero «la validación está siempre en el backend». *Propuesta* (`docs/api.md` §5.2, fila 7): rechazarla con `seal_mismatch` si no cuadra con el prefijo sobre el que cae. *Bloquea:* esa comprobación en el remoto simulado; el resto no.
- **P7. ¿La corrección tiene que ir inmediatamente detrás de su anulación?** La aplicación siempre las escribe juntas, pero ADR-0026 solo dice «que la acompaña en la cola» (`docs/api.md` §5.2). *Propuesta:* exigir que sean contiguas (`pair_not_contiguous`). *Bloquea:* la detección de la pareja (bloque 3) si el plan quiere admitir líneas entre las dos.

Y en `docs/api.md`, para la 015 y **sin bloquear la 014**: la ligadura del dispositivo de la web con su sesión (§5.4), los límites del nombre del dispositivo, la caducidad del código de la consola, cuántos días son «emisiones recientes», la tolerancia del reloj, la duración de la sesión y la ruta para cerrar la sesión de la web (§3).

## 7. Respuestas a las preguntas del prompt

*(Vacío: aquí responderá la dirección.)*
