// The web's sync state in keys of its own of the `ledger` store of IndexedDB,
// step 6 in one transaction (ADR-0026, Part B). Built in block 4 of feature 014.

export const BROWSER_SYNC_STORE = "browser";
